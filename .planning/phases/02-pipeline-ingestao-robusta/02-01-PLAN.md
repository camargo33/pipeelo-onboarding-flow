---
phase: 02-pipeline-ingestao-robusta
plan: 01
type: execute
wave: 2
depends_on: [02-00]
files_modified:
  - C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/src/onboarding-payload.ts
  - C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/src/onboarding-payload.test.ts
  - C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/src/horario.ts
  - C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/api/complete-onboarding.ts
  - C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/api/complete-onboarding.test.ts
  - C:/Users/dopeb/Desktop/admin-pipeelo/app/api/clients/onboarding/create/route.ts
  - C:/Users/dopeb/Desktop/admin-pipeelo/app/api/clients/onboarding/create/route.test.ts
autonomous: true
requirements: [PIPE-01, PIPE-02, PIPE-08]

must_haves:
  truths:
    - "Schema Zod completo descreve o payload real produzido por api/complete-onboarding.ts"
    - "Sender (onboarding-flow) chama OnboardingPayloadSchema.parse() antes de POST e falha 500 estruturado em payload inválido"
    - "Receiver (admin-pipeelo) chama OnboardingPayloadSchema.safeParse() e retorna 400 com lista de issues em payload inválido"
    - "Identificação incompleta (sem CNPJ ou ceo_email) faz receiver responder 400, não 200 silencioso"
    - "Mudar campo no schema sem rebuild do pacote quebra build TS dos dois consumidores"
    - "payload_version: 'v1' é incluído pelo sender e validado pelo receiver"
  artifacts:
    - path: "C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/src/onboarding-payload.ts"
      provides: "OnboardingPayloadSchema completo + tipos derivados"
      contains: "OnboardingPayloadSchema"
    - path: "C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/api/complete-onboarding.ts"
      provides: "Sender que valida payload com Zod antes de POST"
    - path: "C:/Users/dopeb/Desktop/admin-pipeelo/app/api/clients/onboarding/create/route.ts"
      provides: "Receiver com Zod parse + 400 estruturado"
  key_links:
    - from: "api/complete-onboarding.ts"
      to: "pipeelo-onboarding-contracts"
      via: "import { OnboardingPayloadSchema, PAYLOAD_VERSION } + .parse(payload)"
      pattern: "OnboardingPayloadSchema\\.parse"
    - from: "admin-pipeelo/app/api/clients/onboarding/create/route.ts"
      to: "pipeelo-onboarding-contracts"
      via: "import + safeParse + return NextResponse.json({ error, issues }, { status: 400 })"
      pattern: "OnboardingPayloadSchema\\.safeParse"
---

<objective>
Definir schema Zod completo do payload do webhook (PIPE-01), aplicar contract-first validation nos dois lados (PIPE-02), garantir versionamento com `payload_version` (PIPE-08).

Purpose: Schema drift entre repos é Pitfall 10. Phase 1 deixou campos identidade obrigatórios server-side; Phase 2 cristaliza isso num único lugar — o pacote contracts. Daqui pra frente, qualquer campo novo no questionário que precise chegar ao admin-pipeelo PRECISA passar pelo contracts.

Output:
- Schema Zod completo modelando o payload real (session + respostas + horario_semanal expandido)
- Sender (`api/complete-onboarding.ts`) faz `parse()` antes do `fetch` — falha early
- Receiver (`admin-pipeelo/.../create/route.ts`) faz `safeParse()` + retorna 400 com `issues` em vez de 200 silencioso
- Testes Vitest nos 2 lados validando happy path + identification gate
</objective>

<execution_context>
@C:/Users/dopeb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dopeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/.planning/research/PITFALLS.md
@C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/.planning/research/ARCHITECTURE.md
@C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/.planning/codebase/INTEGRATIONS.md
@C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/api/complete-onboarding.ts
@C:/Users/dopeb/Desktop/admin-pipeelo/app/api/clients/onboarding/create/route.ts
@C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/src/onboarding-payload.ts

<interfaces>
<!-- Formato real do payload produzido hoje por api/complete-onboarding.ts -->

```ts
// Payload sender → receiver (formato atual)
{
  session: {
    id: string,                // session.id (DB row id)
    empresa_nome: string,
    ceo_email: string,
    access_token?: string,
    created_at: string,        // ISO
    responsaveis?: Record<string, string>,    // { sac_geral: 'Fulano', financeiro: '...' }
    datas_conclusao?: Record<string, string>, // { sac_geral: ISO, ... }
    cnpj?: string,
    tenant_id?: string | null,
    pipeelo_token?: string | null,
  },
  respostas: {
    identificacao: { [pergunta_id]: any },
    sac_geral: { [pergunta_id]: any },
    financeiro: { [pergunta_id]: any },
    suporte: { [pergunta_id]: any },
    vendas: { [pergunta_id]: any },
    // horario_semanal expandido em segunda_feira/.../domingo/feriado dentro do depto que o tem
  }
}
```

<!-- Departamentos canônicos (de src/types/onboarding.ts e api/_lib/schemas/resposta.ts) -->
DEPARTAMENTOS = ['identificacao', 'sac_geral', 'financeiro', 'suporte', 'vendas']

<!-- Identificação obrigatória (HARD-04 enforced em Phase 1) -->
session.empresa_nome obrigatório
session.ceo_email obrigatório
session.cnpj (14 dígitos) — recomendado obrigatório no payload final

<!-- Receiver atual (admin-pipeelo) já valida session.id + empresa_nome + ceo_email manualmente; vamos substituir por Zod -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir OnboardingPayloadSchema completo + helper de horário</name>
  <files>
    C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/src/onboarding-payload.ts,
    C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/src/onboarding-payload.test.ts,
    C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/src/horario.ts
  </files>
  <behavior>
    - `OnboardingPayloadSchema.parse(validPayload)` retorna parsed object com `payload_version: 'v1'`
    - `OnboardingPayloadSchema.safeParse({ session: { id: 'x' }, respostas: {} })` falha — `empresa_nome`, `ceo_email`, `cnpj` faltando
    - `OnboardingPayloadSchema.safeParse(payloadSemCnpj)` retorna `success: false` com `issues[].path` indicando `['session','cnpj']`
    - `OnboardingPayloadSchema.safeParse(payloadComCnpj13Digitos)` falha (regex 14 digits)
    - `OnboardingPayloadSchema.safeParse(payloadComEmailInvalido)` falha
    - `RespostasByDepartmentSchema` aceita payload com 5 deptos opcionais (identificacao + sac_geral + ... + vendas), cada um sendo `Record<string, unknown>`
    - `payload_version` faltando → default 'v1'
    - `payload_version: 'v2'` (versão futura desconhecida) → safeParse falha (literal restrita)
    - Tipo `OnboardingPayload` exportado e infere shape via `z.infer`
  </behavior>
  <action>
    Substitui o `onboarding-payload.ts` skeleton de 02-00 pelo schema completo modelando o formato real do `api/complete-onboarding.ts`.

    1. Cria `contracts/src/horario.ts` com schema do horário semanal expandido:
       ```ts
       import { z } from 'zod';

       /** Horário em formato HH:MM ou string vazia (dia fechado) */
       const HoraSchema = z.union([
         z.string().regex(/^\d{2}:\d{2}$/),
         z.literal(''),
       ]).optional();

       /** Um dia da semana expandido */
       export const HorarioDiaSchema = z.object({
         abre: HoraSchema,
         fecha: HoraSchema,
         fechado: z.boolean().optional(),
       }).passthrough();

       export const DIAS_SEMANA = [
         'segunda_feira',
         'terca_feira',
         'quarta_feira',
         'quinta_feira',
         'sexta_feira',
         'sabado',
         'domingo',
         'feriado',
       ] as const;
       export type DiaSemana = typeof DIAS_SEMANA[number];
       ```

    2. Substitui `contracts/src/onboarding-payload.ts` por schema completo:
       ```ts
       import { z } from 'zod';

       export const PAYLOAD_VERSION = 'v1' as const;
       export type PayloadVersion = typeof PAYLOAD_VERSION;

       export const DEPARTAMENTOS = [
         'identificacao',
         'sac_geral',
         'financeiro',
         'suporte',
         'vendas',
       ] as const;
       export type Departamento = typeof DEPARTAMENTOS[number];

       /** CNPJ: 14 dígitos numéricos (sem máscara) */
       const CnpjSchema = z.string().regex(/^\d{14}$/, 'CNPJ deve ter 14 dígitos');
       const EmailSchema = z.string().email();
       const IsoDateTimeSchema = z.string().datetime({ offset: true }).or(z.string().min(1));

       export const SessionEnvelopeSchema = z.object({
         id: z.string().min(1),
         empresa_nome: z.string().min(1),
         ceo_email: EmailSchema,
         cnpj: CnpjSchema,
         created_at: IsoDateTimeSchema,
         access_token: z.string().optional(),
         tenant_id: z.string().nullable().optional(),
         pipeelo_token: z.string().nullable().optional(),
         responsaveis: z.record(z.string()).optional(),
         datas_conclusao: z.record(z.string()).optional(),
       }).passthrough();

       export const RespostasDepartmentSchema = z.record(z.unknown());

       export const RespostasByDepartmentSchema = z.object({
         identificacao: RespostasDepartmentSchema.optional(),
         sac_geral: RespostasDepartmentSchema.optional(),
         financeiro: RespostasDepartmentSchema.optional(),
         suporte: RespostasDepartmentSchema.optional(),
         vendas: RespostasDepartmentSchema.optional(),
       }).passthrough();

       export const OnboardingPayloadSchema = z.object({
         payload_version: z.literal(PAYLOAD_VERSION).default(PAYLOAD_VERSION),
         session: SessionEnvelopeSchema,
         respostas: RespostasByDepartmentSchema,
       });

       export type OnboardingPayload = z.infer<typeof OnboardingPayloadSchema>;
       export type SessionEnvelope = z.infer<typeof SessionEnvelopeSchema>;
       ```

    3. Atualiza `contracts/src/index.ts` adicionando export de `horario`:
       ```ts
       export * from './onboarding-payload';
       export * from './horario';
       ```

    4. Reescreve `contracts/src/onboarding-payload.test.ts` cobrindo behavior acima (8+ testes, ver `<behavior>`).

       Fixtures helper:
       ```ts
       const validPayload = {
         session: {
           id: 'sess_abc123',
           empresa_nome: 'ISP Teste LTDA',
           ceo_email: 'ceo@isp.com',
           cnpj: '11222333000181',
           created_at: '2026-05-08T12:00:00Z',
         },
         respostas: {
           identificacao: { empresa_nome: 'ISP Teste LTDA' },
           sac_geral: {},
         },
       };
       ```

    5. Build do pacote: `cd contracts && npm run build`. Confirmar `dist/onboarding-payload.d.ts` exporta `OnboardingPayloadSchema`.

    6. Roda os testes: `cd contracts && npx vitest run`.

    Importante: `OnboardingPayloadSkeletonSchema` da 02-00 é REMOVIDO (substituído pelo Schema completo). Os testes do skeleton em 02-00 serão substituídos pelos novos.
  </action>
  <verify>
    <automated>cd C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts && npm run build && npx vitest run</automated>
  </verify>
  <done>
    - `OnboardingPayloadSchema` exportado e tipado
    - 8+ testes do schema verde
    - `dist/` rebuild contém o novo schema
    - `OnboardingPayload` type exportado via `z.infer`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sender — validar payload no onboarding-flow antes de POST</name>
  <files>
    C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/api/complete-onboarding.ts,
    C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/api/complete-onboarding.test.ts
  </files>
  <behavior>
    - Handler chama `OnboardingPayloadSchema.parse({ payload_version: 'v1', session, respostas })` ANTES do fetch ao admin-pipeelo
    - Se parse falhar (ex: cnpj sem 14 dígitos): retorna 500 com `{ error: 'invalid_outbound_payload', issues: [...] }`. Erro logado mas com PII mascarada (não logar respostas brutas).
    - Se parse passar: fetch para admin-pipeelo com body JSON contendo o payload validado
    - Header `Idempotency-Key: <session.id>` adicionado ao fetch
    - 200 do admin → handler responde 200 ao caller
    - 4xx/5xx do admin → handler responde 502 (`{ error: 'admin_webhook_failed', status, body }`) — NÃO 200 silencioso
    - Endpoint legacy `complete-onboarding` continua funcionando para chamadas atuais do front (compat)
  </behavior>
  <action>
    Migra `api/complete-onboarding.ts` para validar payload via Zod do contracts antes de fazer POST ao admin.

    1. Lê `api/complete-onboarding.ts` atual com Read tool. Identifica o ponto onde monta o body do webhook (geralmente após agregar respostas + expandir horário).

    2. Adiciona import:
       ```ts
       import {
         OnboardingPayloadSchema,
         PAYLOAD_VERSION,
       } from 'pipeelo-onboarding-contracts';
       ```

    3. ANTES do `fetch(adminUrl, ...)`:
       ```ts
       const payload = {
         payload_version: PAYLOAD_VERSION,
         session: {
           id: session.id,
           empresa_nome: session.empresa_nome,
           ceo_email: session.ceo_email,
           cnpj: session.cnpj,
           created_at: session.created_at,
           access_token: session.access_token,
           tenant_id: session.tenant_id ?? null,
           pipeelo_token: session.pipeelo_token ?? null,
           responsaveis: session.responsaveis,
           datas_conclusao: session.datas_conclusao,
         },
         respostas: respostasAggregadas,
       };

       const parseResult = OnboardingPayloadSchema.safeParse(payload);
       if (!parseResult.success) {
         console.error('[complete-onboarding] invalid_outbound_payload', {
           sessionId: session.id,
           issues: parseResult.error.issues.map(i => ({ path: i.path, code: i.code })),
         });
         return res.status(500).json({
           error: 'invalid_outbound_payload',
           issues: parseResult.error.issues,
         });
       }
       const validatedPayload = parseResult.data;
       ```

    4. No fetch:
       ```ts
       const response = await fetch(`${adminUrl}/api/clients/onboarding/create`, {
         method: 'POST',
         headers: {
           'Content-Type': 'application/json',
           'Authorization': `Bearer ${process.env.ONBOARDING_WEBHOOK_TOKEN}`,
           'Idempotency-Key': session.id,
         },
         body: JSON.stringify(validatedPayload),
         keepalive: true,
       });
       if (!response.ok) {
         const body = await response.text().catch(() => '');
         console.error('[complete-onboarding] admin webhook failed', {
           status: response.status,
           sessionId: session.id,
         });
         return res.status(502).json({
           error: 'admin_webhook_failed',
           status: response.status,
           body: body.slice(0, 500),
         });
       }
       ```

    5. Cria `api/complete-onboarding.test.ts` (mock fetch + mock supabase) com 4 testes:
       - 500 invalid_outbound_payload quando session sem cnpj
       - 502 admin_webhook_failed quando admin retorna 400
       - 200 happy path com fetch chamado com Idempotency-Key
       - payload enviado contém `payload_version: 'v1'`

       Pattern de mock segue `api/sessions/*.test.ts` (Phase 1):
       ```ts
       import { describe, it, expect, vi, beforeEach } from 'vitest';
       vi.mock('../api/_lib/supabase', () => ({
         requireSupabase: vi.fn(),
         getServiceSupabase: vi.fn(),
       }));
       ```

    6. Roda `npx vitest run api/complete-onboarding.test.ts`.

    Pitfall: NÃO logar `payload.respostas` nem `session.cnpj`/`ceo_email` em logs (LGPD). Logar só `sessionId` + `issues[].path`.
  </action>
  <verify>
    <automated>cd C:/Users/dopeb/Desktop/pipeelo-onboarding-flow && npx vitest run api/complete-onboarding.test.ts</automated>
  </verify>
  <done>
    - `OnboardingPayloadSchema.safeParse` é chamado antes do fetch
    - 500 invalid_outbound_payload em payload inválido
    - 502 admin_webhook_failed em erro do admin
    - Header `Idempotency-Key: <session.id>` enviado
    - 4 testes verde
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Receiver — Zod parse + 400 estruturado no admin-pipeelo</name>
  <files>
    C:/Users/dopeb/Desktop/admin-pipeelo/app/api/clients/onboarding/create/route.ts,
    C:/Users/dopeb/Desktop/admin-pipeelo/app/api/clients/onboarding/create/route.test.ts
  </files>
  <behavior>
    - Sem header Authorization Bearer → 401
    - Token inválido → 401
    - Body sem `payload_version` → safeParse aplica default `v1` (ok)
    - Body com `payload_version: 'v2'` → 400 com issue indicando literal failure
    - Body sem `session.cnpj` → 400 com `issues[].path = ['session','cnpj']`
    - Body sem `session.ceo_email` → 400
    - Body com `ceo_email` malformado → 400 com `issues[].code = 'invalid_string'`
    - Body válido → 200 ou 201 (created/updated) — comportamento atual preservado, exceto agora usa shape do Zod parsed
    - Resposta 400 inclui `{ error: 'invalid_payload', issues: [...] }` (não string genérica)
  </behavior>
  <action>
    Substitui validação manual atual pelo `OnboardingPayloadSchema.safeParse`.

    1. Lê `app/api/clients/onboarding/create/route.ts` (já lido em context).

    2. Adiciona import no topo:
       ```ts
       import {
         OnboardingPayloadSchema,
         type OnboardingPayload,
       } from 'pipeelo-onboarding-contracts';
       ```

    3. Remove a interface inline `OnboardingPayload` (linhas 17-20) — agora vem do contracts.

    4. Substitui o bloco `// 2. Validar payload` (após auth) por:
       ```ts
       const rawBody = await request.json().catch(() => null);
       if (!rawBody) {
         return NextResponse.json({ error: 'invalid_json' }, { status: 400 });
       }

       const parseResult = OnboardingPayloadSchema.safeParse(rawBody);
       if (!parseResult.success) {
         console.warn('[onboarding/create] invalid_payload', {
           issues: parseResult.error.issues.map(i => ({ path: i.path, code: i.code })),
         });
         return NextResponse.json(
           {
             error: 'invalid_payload',
             issues: parseResult.error.issues,
           },
           { status: 400 }
         );
       }

       const { session, respostas, payload_version } = parseResult.data;
       ```

    5. Mantém o resto do handler (verificação se sessão existe → update / insert) funcionando com os campos validados. Remove a checagem manual `if (!session.id || !session.empresa_nome || !session.ceo_email)` — Zod já garantiu.

    6. No insert/update inclui `payload_version` no record (se a coluna não existir na tabela, gravar em `respostas.__meta = { payload_version }` ou ignorar silenciosamente — receiver atual não precisa persistir esse campo, mas vamos passar adiante via `respostas`). Deixar TODO inline:
       ```ts
       // TODO Plan 02-02: persistir payload_version + status state machine
       ```

    7. Cria `app/api/clients/onboarding/create/route.test.ts`:
       - Mock `@/lib/supabase` (objeto com `from()`)
       - Mock `process.env.ONBOARDING_WEBHOOK_TOKEN = 'test-token'`
       - Helper `makeRequest(body, token)` que retorna `NextRequest` mockado
       - 6 testes cobrindo behaviors acima (401 sem auth, 400 sem cnpj, 400 ceo_email inválido, 400 payload_version: 'v2', 201 happy path, 200 update path)

       Pattern de mock NextRequest:
       ```ts
       function makeReq(body: any, token = 'test-token') {
         return new Request('http://localhost/api/clients/onboarding/create', {
           method: 'POST',
           headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' },
           body: JSON.stringify(body),
         }) as any;
       }
       ```

    8. Roda `npm test -- create/route.test.ts` no admin.

    Importante: receiver continua usando `WEBHOOK_TOKEN` env. Não tocar em rotação de token aqui (Plan 02-03).
  </action>
  <verify>
    <automated>cd C:/Users/dopeb/Desktop/admin-pipeelo && npm test -- create/route.test.ts</automated>
  </verify>
  <done>
    - Receiver usa `OnboardingPayloadSchema.safeParse`
    - 400 estruturado com `issues` em payload inválido
    - 401 mantido para auth fail
    - 6 testes verde
    - Identificação incompleta (sem CNPJ ou email) bloqueada explicitamente
  </done>
</task>

</tasks>

<verification>
- Schema contract definido em 1 lugar e importado pelos 2 repos
- `npm test` em ambos repos verde
- Mudar campo no schema sem rebuild do contracts → quebra compile dos consumidores (verificável manualmente: comentar export, build admin → erro TS)
- Identificação obrigatória enforced no boundary
</verification>

<success_criteria>
- PIPE-01: schema único compartilhado (FEITO via pacote contracts)
- PIPE-02: receiver retorna 400 estruturado em payload inválido
- PIPE-08: `payload_version: 'v1'` validado em ambos lados
- Suite Phase 1 não regrediu (47+ tests onboarding-flow)
- Admin-pipeelo: 8+ testes (sanity 02-00 + 6 do create/route)
</success_criteria>

<output>
`.planning/phases/02-pipeline-ingestao-robusta/02-01-SUMMARY.md`
</output>
