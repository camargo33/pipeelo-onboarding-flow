---
phase: 02-pipeline-ingestao-robusta
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/package.json
  - C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/tsconfig.json
  - C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/src/index.ts
  - C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/src/onboarding-payload.ts
  - C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/src/onboarding-payload.test.ts
  - C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/README.md
  - C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/package.json
  - C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/tsconfig.json
  - C:/Users/dopeb/Desktop/admin-pipeelo/package.json
  - C:/Users/dopeb/Desktop/admin-pipeelo/vitest.config.ts
  - C:/Users/dopeb/Desktop/admin-pipeelo/tests/setup.ts
  - C:/Users/dopeb/Desktop/admin-pipeelo/tests/contracts/sanity.test.ts
autonomous: true
requirements: [PIPE-01]

must_haves:
  truths:
    - "O pacote `pipeelo-onboarding-contracts` resolve em ambos os repos via import"
    - "Build TypeScript compila o pacote em `dist/` com .d.ts gerados"
    - "Vitest roda no admin-pipeelo executando ao menos 1 teste do contrato"
    - "Vitest existente no onboarding-flow continua passando após adição do workspace"
  artifacts:
    - path: "C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/package.json"
      provides: "Pacote npm `pipeelo-onboarding-contracts` com main/types apontando pra dist"
    - path: "C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/src/onboarding-payload.ts"
      provides: "Schema Zod skeleton + PAYLOAD_VERSION exportados"
    - path: "C:/Users/dopeb/Desktop/admin-pipeelo/vitest.config.ts"
      provides: "Vitest infra no admin-pipeelo"
  key_links:
    - from: "C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/package.json"
      to: "contracts/dist"
      via: "workspaces + file dependency"
      pattern: "pipeelo-onboarding-contracts"
    - from: "C:/Users/dopeb/Desktop/admin-pipeelo/package.json"
      to: "../pipeelo-onboarding-flow/contracts"
      via: "file: dependency"
      pattern: "pipeelo-onboarding-contracts"
---

<objective>
Wave 0 — Setup do pacote `pipeelo-onboarding-contracts` (workspace local consumido pelos 2 repos via `file:` dependency) + scaffold de Vitest no admin-pipeelo.

Este plan **não** define o schema completo (esse vem em 02-01). Aqui apenas: estrutura de pacote, build TS, exports, e infra de teste no admin para que 02-01 e 02-02 possam executar testes.

Purpose: Sem o pacote contracts e sem Vitest no admin, qualquer mudança em 02-01+ exige re-scaffold no meio da execução. Wave 0 é pré-condição inegociável.

Output:
- Pacote `pipeelo-onboarding-contracts` v0.1.0 com skeleton schema + build TS funcional
- onboarding-flow consome via npm workspace (`workspaces: ["contracts"]`)
- admin-pipeelo consome via `"pipeelo-onboarding-contracts": "file:../pipeelo-onboarding-flow/contracts"`
- Vitest 4.x configurado no admin-pipeelo + 1 sanity test
</objective>

<execution_context>
@C:/Users/dopeb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dopeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/.planning/PROJECT.md
@C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/.planning/ROADMAP.md
@C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/.planning/STATE.md
@C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/.planning/research/STACK.md
@C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/.planning/research/ARCHITECTURE.md
@C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/.planning/research/PITFALLS.md

<interfaces>
<!-- Estado atual relevante para Wave 0 -->

`pipeelo-onboarding-flow/package.json` — Vite 5 + Vitest 4 já instalados. NÃO tem `workspaces` declarado. Adicionar workspaces requer testar que vitest existente (onboarding-flow root) continua funcionando.

`pipeelo-onboarding-flow/api/complete-onboarding.ts` — gera payload atual com formato:
```ts
{
  session: { id, empresa_nome, ceo_email, access_token?, created_at, responsaveis?, datas_conclusao? },
  respostas: Record<string, any>  // agrupado por departamento, com horario_semanal expandido
}
```
Esse payload É a base do schema Zod a ser definido em 02-01. Em 02-00 apenas declaramos o **skeleton** (z.object passthrough) + `payload_version: 'v1'` constante.

`admin-pipeelo/package.json` — Next.js 16, Vitest 4.1.5 já em devDeps + script `"test": "vitest run"`. NÃO tem `vitest.config.ts` nem testes ainda.

`admin-pipeelo/app/api/clients/onboarding/create/route.ts` (linha 17-20) — define interface `OnboardingPayload` inline (será substituída em 02-01 pelo Zod parse).

`pipeelo-onboarding-contracts` — não existe ainda; vamos criar dentro do repo onboarding-flow em `contracts/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar pacote contracts skeleton no onboarding-flow</name>
  <files>
    C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/package.json,
    C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/tsconfig.json,
    C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/src/index.ts,
    C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/src/onboarding-payload.ts,
    C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/src/onboarding-payload.test.ts,
    C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts/README.md,
    C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/package.json
  </files>
  <action>
    Cria o subpacote `contracts/` no repo onboarding-flow.

    1. `contracts/package.json`:
       ```json
       {
         "name": "pipeelo-onboarding-contracts",
         "version": "0.1.0",
         "private": true,
         "main": "./dist/index.js",
         "module": "./dist/index.js",
         "types": "./dist/index.d.ts",
         "exports": {
           ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js", "require": "./dist/index.js" }
         },
         "files": ["dist", "src"],
         "scripts": {
           "build": "tsc -p tsconfig.json",
           "test": "vitest run"
         },
         "dependencies": {
           "zod": "^3.25.0"
         },
         "devDependencies": {
           "typescript": "^5.8.0",
           "vitest": "^4.1.0"
         }
       }
       ```

    2. `contracts/tsconfig.json`:
       ```json
       {
         "compilerOptions": {
           "target": "ES2022",
           "module": "ESNext",
           "moduleResolution": "Bundler",
           "lib": ["ES2022", "DOM"],
           "declaration": true,
           "declarationMap": true,
           "outDir": "./dist",
           "rootDir": "./src",
           "strict": true,
           "esModuleInterop": true,
           "skipLibCheck": true,
           "forceConsistentCasingInFileNames": true,
           "resolveJsonModule": true
         },
         "include": ["src/**/*.ts"],
         "exclude": ["dist", "node_modules", "**/*.test.ts"]
       }
       ```

    3. `contracts/src/onboarding-payload.ts` — SKELETON apenas (schema completo vem em 02-01):
       ```ts
       import { z } from 'zod';

       /** Versão do payload — incrementar quando schema break-changes */
       export const PAYLOAD_VERSION = 'v1' as const;
       export type PayloadVersion = typeof PAYLOAD_VERSION;

       /**
        * Skeleton — schema completo será definido em Plan 02-01 com base em
        * api/complete-onboarding.ts (formato atual de payload).
        * Por ora aceita qualquer shape com session.id obrigatório (sanity guard).
        */
       export const OnboardingPayloadSkeletonSchema = z.object({
         payload_version: z.literal(PAYLOAD_VERSION).default(PAYLOAD_VERSION),
         session: z.object({
           id: z.string().min(1),
         }).passthrough(),
         respostas: z.record(z.unknown()),
       });

       export type OnboardingPayloadSkeleton = z.infer<typeof OnboardingPayloadSkeletonSchema>;
       ```

    4. `contracts/src/index.ts`:
       ```ts
       export * from './onboarding-payload';
       ```

    5. `contracts/src/onboarding-payload.test.ts`:
       ```ts
       import { describe, it, expect } from 'vitest';
       import { OnboardingPayloadSkeletonSchema, PAYLOAD_VERSION } from './onboarding-payload';

       describe('OnboardingPayloadSkeletonSchema', () => {
         it('exporta PAYLOAD_VERSION = v1', () => {
           expect(PAYLOAD_VERSION).toBe('v1');
         });

         it('aceita payload mínimo com session.id e respostas', () => {
           const r = OnboardingPayloadSkeletonSchema.safeParse({
             session: { id: 'abc' },
             respostas: {},
           });
           expect(r.success).toBe(true);
         });

         it('rejeita payload sem session.id', () => {
           const r = OnboardingPayloadSkeletonSchema.safeParse({
             session: {},
             respostas: {},
           });
           expect(r.success).toBe(false);
         });

         it('default payload_version = v1', () => {
           const r = OnboardingPayloadSkeletonSchema.parse({
             session: { id: 'x' },
             respostas: {},
           });
           expect(r.payload_version).toBe('v1');
         });
       });
       ```

    6. `contracts/README.md` — 1 parágrafo explicando: "Pacote compartilhado entre `pipeelo-onboarding-flow` (sender) e `admin-pipeelo` (receiver) com schemas Zod do webhook. Versionar via `PAYLOAD_VERSION`. Build com `npm run build` antes de commit."

    7. Atualiza `pipeelo-onboarding-flow/package.json` raiz adicionando workspace:
       ```json
       "workspaces": ["contracts"]
       ```
       (manter todas as outras chaves intactas — usar `Read` antes de editar)

    8. Roda `npm install` no root do onboarding-flow para linkar workspace.

    9. Roda `cd contracts && npm run build` para gerar `dist/` (deve gerar `dist/index.js`, `dist/index.d.ts`, `dist/onboarding-payload.js`, `dist/onboarding-payload.d.ts`).

    10. Roda `npx vitest run contracts/src/onboarding-payload.test.ts` (do root) para confirmar testes do contrato passam.

    11. Roda `npm test -- --run` no root onboarding-flow para garantir que suite existente (47 passed da Phase 1) continua verde.

    Importante: `tsconfig.json` raiz do onboarding-flow (Vite SPA) NÃO precisa mudar — o pacote contracts tem seu próprio tsconfig isolado.
  </action>
  <verify>
    <automated>cd C:/Users/dopeb/Desktop/pipeelo-onboarding-flow/contracts && npm run build && npx vitest run && cd .. && npm test -- --run</automated>
  </verify>
  <done>
    - `contracts/dist/index.js` e `contracts/dist/index.d.ts` existem
    - `contracts/package.json` declara `name: pipeelo-onboarding-contracts`
    - `pipeelo-onboarding-flow/package.json` tem `workspaces: ["contracts"]`
    - 4 testes do contrato passam
    - Suite Phase 1 do onboarding-flow continua verde
  </done>
</task>

<task type="auto">
  <name>Task 2: Linkar contracts no admin-pipeelo + scaffold Vitest</name>
  <files>
    C:/Users/dopeb/Desktop/admin-pipeelo/package.json,
    C:/Users/dopeb/Desktop/admin-pipeelo/vitest.config.ts,
    C:/Users/dopeb/Desktop/admin-pipeelo/tests/setup.ts,
    C:/Users/dopeb/Desktop/admin-pipeelo/tests/contracts/sanity.test.ts
  </files>
  <action>
    Conecta admin-pipeelo ao pacote contracts e instala infra de testes.

    1. Edita `C:/Users/dopeb/Desktop/admin-pipeelo/package.json` (Read antes de Edit):
       - Adiciona em `dependencies`:
         ```json
         "pipeelo-onboarding-contracts": "file:../pipeelo-onboarding-flow/contracts"
         ```
       - Mantém Vitest 4.1.5 já presente em devDependencies. Não mexer nas outras deps.

    2. Cria `C:/Users/dopeb/Desktop/admin-pipeelo/vitest.config.ts`:
       ```ts
       import { defineConfig } from 'vitest/config';
       import path from 'node:path';

       export default defineConfig({
         test: {
           globals: true,
           environment: 'node',
           include: ['tests/**/*.test.ts', 'app/**/*.test.ts', 'lib/**/*.test.ts'],
           setupFiles: ['./tests/setup.ts'],
         },
         resolve: {
           alias: {
             '@': path.resolve(__dirname, '.'),
           },
         },
       });
       ```

    3. Cria `C:/Users/dopeb/Desktop/admin-pipeelo/tests/setup.ts`:
       ```ts
       // Test setup global para admin-pipeelo
       // Phase 2 — Plan 02-00 Wave 0
       import { afterEach, vi } from 'vitest';

       afterEach(() => {
         vi.restoreAllMocks();
         vi.unstubAllEnvs();
       });
       ```

    4. Cria `C:/Users/dopeb/Desktop/admin-pipeelo/tests/contracts/sanity.test.ts`:
       ```ts
       import { describe, it, expect } from 'vitest';
       import {
         OnboardingPayloadSkeletonSchema,
         PAYLOAD_VERSION,
       } from 'pipeelo-onboarding-contracts';

       describe('admin-pipeelo: pipeelo-onboarding-contracts wiring', () => {
         it('resolve PAYLOAD_VERSION do pacote contracts', () => {
           expect(PAYLOAD_VERSION).toBe('v1');
         });

         it('schema skeleton aceita payload mínimo', () => {
           const r = OnboardingPayloadSkeletonSchema.safeParse({
             session: { id: 'sess_abc' },
             respostas: {},
           });
           expect(r.success).toBe(true);
         });
       });
       ```

    5. No diretório admin-pipeelo: `npm install` para resolver `file:../pipeelo-onboarding-flow/contracts` e popular `node_modules/pipeelo-onboarding-contracts/dist/`.

    6. Roda `npm test` (= `vitest run`) no admin-pipeelo. Deve achar 2 testes na suite sanity + executar o setup.

    Atenção: Next.js 16 + Vitest 4 podem ter conflito de tipos React 19. Se aparecer erro de tipos no setup, adicionar `tsconfig` test override é OUT OF SCOPE deste plan (criar issue/TODO no SUMMARY). O critério é que `npm test` rode os 2 testes verde — se TS reclamar mas runtime passar, tá OK.
  </action>
  <verify>
    <automated>cd C:/Users/dopeb/Desktop/admin-pipeelo && npm install && npm test</automated>
  </verify>
  <done>
    - `vitest.config.ts` existe no admin-pipeelo
    - `node_modules/pipeelo-onboarding-contracts/dist/index.js` existe (link via file:)
    - `npm test` executa e os 2 testes da sanity suite passam
    - `tests/setup.ts` é carregado (verificável por log ou pela ausência de erros)
  </done>
</task>

</tasks>

<verification>
**Pós-execução:**
- `cd contracts && npm run build` → exit 0, dist/ populado
- `npx vitest run contracts/src/onboarding-payload.test.ts` no onboarding-flow → 4 passed
- `cd admin-pipeelo && npm test` → ≥2 passed (sanity wire test)
- `npm test -- --run` no onboarding-flow → 47+ passed (Phase 1 não regrediu)
</verification>

<success_criteria>
- Pacote `pipeelo-onboarding-contracts` v0.1.0 com build funcional
- onboarding-flow consume via workspace, admin-pipeelo via `file:` dep
- Vitest scaffold no admin-pipeelo executando 2+ testes
- Phase 1 do onboarding-flow não regrediu (suite verde)
- Pronto para Plan 02-01 definir schema real
</success_criteria>

<output>
Após completion: `.planning/phases/02-pipeline-ingestao-robusta/02-00-SUMMARY.md`
</output>
