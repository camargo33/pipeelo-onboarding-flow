---
phase: 07-setupper-studio
plan: 07
type: execute
wave: 5
depends_on: [07-01, 07-04]
files_modified:
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/agenda.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/agenda.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/lint.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/lint.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/session-invariants.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/session-invariants.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/final-map.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/final-map.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/blueprint.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/tools.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/tools.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/reminders.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/system-prompt.ts
autonomous: true

must_haves:
  truths:
    - "Implementa a 07-GUIDANCE-SPEC integralmente; em dúvida, a spec decide"
    - "computeAgenda cobre 100% das perguntas do modo da sessão (cada pergunta em exatamente 1 missão) e a missão atual é a primeira incompleta"
    - "renderSessionContext renderiza Progresso + MISSÃO ATUAL completa + próximas (títulos) + 'já configurado' compacto — SEM a lista integral de pendências do departamento"
    - "search_questions encontra pergunta por keyword e informa se já foi respondida (com o valor)"
    - "lintAssistantText detecta V1–V5 e as violações viram no MÁXIMO 1 reminder no turno seguinte (prioridade R1 > lint > R2, cap global 2)"
    - "confirm_flow anexa warnings de invariantes INV1/INV2/INV4; complete_department bloqueia INV3 e exige recap ≥200 chars; o ÚLTIMO departamento exige confirmacao_final=true e sem ela devolve o mapa final renderizado"
    - "O mapa final é persistido como insight mapa_final ao concluir a sessão"
  artifacts:
    - path: "api/_lib/agent/agenda.ts"
      provides: "computeAgenda + TOPIC_MAP + tipos Mission/Agenda"
      contains: "computeAgenda"
    - path: "api/_lib/agent/lint.ts"
      provides: "lintAssistantText V1–V5"
      contains: "lintAssistantText"
    - path: "api/_lib/agent/session-invariants.ts"
      provides: "computeSessionInvariants INV1–INV5"
    - path: "api/_lib/agent/final-map.ts"
      provides: "renderFinalMap(state) — a planta do atendimento em markdown"
  key_links:
    - from: "api/_lib/agent/blueprint.ts"
      to: "api/_lib/agent/agenda.ts"
      via: "renderSessionContext usa computeAgenda para a seção MISSÃO ATUAL"
      pattern: "computeAgenda"
    - from: "api/_lib/agent/reminders.ts"
      to: "api/_lib/agent/lint.ts"
      via: "violations de lint entram no computeReminders com prioridade entre R1 e R2"
      pattern: "lintAssistantText"
    - from: "api/_lib/agent/tools.ts"
      to: "api/_lib/agent/session-invariants.ts"
      via: "warnings em confirmFlow + gate INV3/recap/confirmacao_final em completeDepartment"
      pattern: "computeSessionInvariants"
---

<objective>
Implementar o motor de condução da 07-GUIDANCE-SPEC: agenda de missões com curadoria de contexto, tool search_questions, linter de qualidade de resposta, invariantes de sessão e o fechamento ponta-a-ponta com mapa final. É o plano que transforma o setupper de "coletor com gates" em condutor que entrega o agente MONTADO.
</objective>

<context>
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/.planning/phases/07-setupper-studio/07-GUIDANCE-SPEC.md
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/.planning/phases/07-setupper-studio/07-HARNESS-SPEC.md
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/blueprint.ts
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/tools.ts
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/src/lib/questions.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agenda.ts — missões + TOPIC_MAP + cobertura total</name>
  <files>api/_lib/agent/agenda.ts, api/_lib/agent/agenda.test.ts</files>
  <behavior>
    - Tipos Mission/Agenda EXATOS da spec §1.1. `computeAgenda(state)` com completude determinística por tipo de missão.
    - `TOPIC_MAP`: construir lendo questions.json INTEIRO — começar pelas amarrações canônicas da spec §1.2 e completar TODAS as perguntas pelo critério dado (pergunta entra no fluxo que a citaria; sobras viram perguntas_tema por seção do questionário, com titulo da seção). Grupo `credenciais_erp` isolado como perguntas_tema própria (justificativa: bloco sensível pedido de uma vez).
    - Ordem das missões por dept: abertura → (estilo se sac_geral) → montar_fluxo/perguntas_tema na ordem do catálogo/seções → recap_conclusao. `mapa_final` entra quando todos os depts exigidos menos 1 estão concluídos.
    - Perguntas condicionais: questionIds de uma missão são filtrados por visibilidade ATUAL (conditional.ts) na hora do render — a agenda referencia ids; a visibilidade é avaliada no consumo.
    - Testes: (a) COBERTURA — união dos questionIds de todas as missões (modo completo) == conjunto de todas as perguntas de questions.json (asserção de igualdade de sets; se falhar, o teste imprime as faltantes); idem modo comercial (identificacao+vendas); (b) missão atual avança quando fluxo confirmado+obrigatórias respondidas; (c) mapa_final aparece só no fim.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2: curadoria do contexto + search_questions</name>
  <files>api/_lib/agent/blueprint.ts, api/_lib/agent/tools.ts, api/_lib/agent/tools.test.ts</files>
  <behavior>
    - `renderSessionContext` reescrito para o layout da spec §1.3 (Progresso / MISSÃO ATUAL / Próximas / Já configurado / Resumo). A MISSÃO ATUAL rende: fluxo padrão + decisões (com padrões) + perguntas ligadas VISÍVEIS completas (id, texto, tipo, opcoes com values, hint de formato — reusar o render de pergunta existente). REMOVER a listagem integral de pendências do departamento.
    - Tool nova `search_questions` (schema: `{texto: string}`): busca normalizada (lowercase, sem acentos) por substring/keywords sobre pergunta+id+opcoes de questions.json; retorna cap 8 `{pergunta_id, pergunta, tipo, opcoes?, departamento, respondida, valor_atual?}` (valor omitido para SENSITIVE_PREFIXES); zero matches → `{matches: [], dica: 'tente outro termo; puxe pelo assunto'}`. Registrar em AGENT_TOOLS + executor. Read-only.
    - Teste de tamanho (critério falsificável §8.1): fixture de sessão com 100 respostas → `renderSessionContext().length / 4` ≤ 2.500 tokens aproximados (asserção em chars: ≤10.000).
    - Testes search: acha por termo do enunciado; acha por value de opção ("asaas" → gateway_pagamento); respondida traz valor; sensível não traz valor.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lint.ts + integração nos reminders</name>
  <files>api/_lib/agent/lint.ts, api/_lib/agent/lint.test.ts, api/_lib/agent/reminders.ts</files>
  <behavior>
    - `lintAssistantText(texto, state)` com V1–V5 EXATOS da spec §3 (detecções e textos de reminder da tabela). V3: extrair frases interrogativas do texto; keywords >4 chars sem stopwords PT; overlap ≥0,6 contra enunciados de perguntas RESPONDIDAS; reminder inclui pergunta e valor salvo.
    - `computeReminders` passa a receber o lint do ÚLTIMO texto do assistant: prioridade R1 > lint (1 no máx) > R2; cap global 2/turno mantido.
    - Testes: cada V dispara/não-dispara com exemplos reais (mensagem com 5 '?', parede de 1.500 chars, re-pergunta de CNPJ respondido, texto citando "confirm_flow", formulário de 6 linhas-pergunta); prioridade e caps.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 4: session-invariants.ts + gates no confirm/complete</name>
  <files>api/_lib/agent/session-invariants.ts, api/_lib/agent/session-invariants.test.ts, api/_lib/agent/tools.ts, api/_lib/agent/tools.test.ts</files>
  <behavior>
    - `computeSessionInvariants(state)` com INV1–INV5 da spec §4 (matching por keywords normalizadas entre textos de decisões; INV3 é crisp: fluxo confirmado + pergunta de horário sem resposta).
    - `confirmFlow`: após sucesso, anexar `warnings` de INV1/INV2/INV4 RELEVANTES ao fluxo confirmado (formato `[INVn] ...` acionável).
    - `completeDepartment`: (a) INV3 bloqueia sac_geral com erro instrutivo; (b) novo arg obrigatório `recap: string` ≥200 chars → insight `recap_etapa` (categoria nova na lista); (c) ÚLTIMO dept exigido pelo modo: sem `confirmacao_final: true` → retorna `{ final_review_required: true, mapa: renderFinalMap(state), pendencias_invariantes }` SEM concluir; com ela → conclui e persiste insight `mapa_final` com o render.
    - Retrocompat de schema: `recap`/`confirmacao_final` adicionados ao JSON schema da tool (recap required; confirmacao_final não — só exigida pelo gate no último dept).
    - Testes: INV1 menu×roteamento com item órfão → warning; INV3 bloqueia e o erro cita a pergunta de horário; complete sem recap → erro; último dept sem confirmacao_final → mapa devolvido e status NÃO muda; com ela → conclui + insight mapa_final existe.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 5: final-map.ts + contrato + system prompt v2</name>
  <files>api/_lib/agent/final-map.ts, api/_lib/agent/final-map.test.ts, api/_lib/agent/system-prompt.ts</files>
  <behavior>
    - `renderFinalMap(state): string` — markdown da spec §5.1 (identidade+estilo; agentes por dept com fluxos confirmados: título+passos+decisões-chave; roteamento menu→destino; horários; integrações declaradas (ERP/gateway — NUNCA credenciais); planos; insights de personalização). Determinístico; seções vazias omitidas com honestidade ("(nenhum fluxo custom)").
    - Payload: adicionar `final_map?: string` ao contrato (coordenar com 07-05 Task 1 — se já executada, editar flow-spec.ts/complete-onboarding.ts adicionando o campo aditivo; buscar o insight mapa_final).
    - System prompt v2 (spec §6): reescrever "Como conduzir" em torno da MISSÃO ATUAL; adicionar o exemplar few-shot compacto (~25 linhas, troca de cancelamento com rascunho citado + 1 decisão com recomendação); contrato de ritmo; instruções de recap e confirmacao_final; instrução do search_questions para informação voluntária. MANTER o restante intacto (estabilidade).
    - Testes do final-map: com fixture completa contém as seções e decisões; nunca contém valores de perguntas SENSITIVE_PREFIXES.
  </behavior>
</task>

</tasks>

<verification>
- `npm test` + `npm run build` verdes; teste de cobertura da agenda passa nos 2 modos.
- Percorrer os critérios falsificáveis da spec §8 (1 e 2 são testes; 3 fica para os evals do 07-06).
- Manual: conversa dev — conferir que o contexto mostra UMA missão; adiantar "usamos Asaas" fora da missão e ver search+save+retorno; concluir tudo e receber o mapa final antes do último complete.
</verification>
