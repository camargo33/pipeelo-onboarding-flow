---
phase: 07-setupper-studio
plan: 05
type: execute
wave: 5
depends_on: [07-01]
files_modified:
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/contracts/src/flow-spec.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/contracts/src/flow-spec.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/contracts/src/onboarding-payload.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/complete-onboarding.ts
  - C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/app/api/clients/onboarding/create/route.ts
  - C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/lib/onboarding/v2/flow-spec.ts
  - C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/lib/onboarding/v2/flow-spec.test.ts
  - C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/lib/onboarding/agent/prompt-generator.ts
autonomous: false

must_haves:
  truths:
    - "FlowSpecSchema/StyleSpecSchema vivem em contracts/ e validam nos DOIS lados (sender parse, receiver safeParse) — padrão Phase 02"
    - "complete-onboarding monta flows[] a partir dos insights fluxo_confirmado (payload JSONB) e style a partir do estilo_comunicacao — payload continua v1 (campos aditivos opcionais)"
    - "No admin, ONBOARDING_FLOWSPEC=off (default) ignora flows/style sem NENHUMA mudança de comportamento vs hoje"
    - "Com a flag on, o prompt do subagente gerador inclui os FlowSpecs do target e instrução de materializá-los como Casos; pós-geração, verificação determinística reporta fluxo confirmado ausente do prompt gerado"
    - "Session replay de uma sessão real com fluxos confirmados evidencia a diferença antes de ligar a flag em produção"
  artifacts:
    - path: "contracts/src/flow-spec.ts"
      provides: "FlowSpecSchema + StyleSpecSchema + tipos"
      contains: "FlowSpecSchema"
    - path: "admin-pipeelo/lib/onboarding/v2/flow-spec.ts"
      provides: "renderFlowSpecsForPrompt + verifyFlowPresence + flowspecEnabled"
  key_links:
    - from: "api/complete-onboarding.ts"
      to: "contracts/src/flow-spec.ts"
      via: "FlowSpecSchema no OnboardingPayloadSchema"
      pattern: "flows"
    - from: "admin-pipeelo/lib/onboarding/agent/prompt-generator.ts"
      to: "admin-pipeelo/lib/onboarding/v2/flow-spec.ts"
      via: "renderFlowSpecsForPrompt no user prompt do subagente"
      pattern: "renderFlowSpecsForPrompt"
---

<objective>
Fechar o ciclo: o que o cliente confirmou no estúdio vira contrato estruturado que o gerador de prompts do admin materializa como Casos de Atendimento — rastreável e verificável.

⚠️ CHANGE CONTROL: este plano toca o gerador de prompts do admin (afeta TODO onboarding futuro). `autonomous: false` — o lado ADM exige: (1) flag OFF por default; (2) replay de sessão real como evidência; (3) revisão humana do diff ANTES de commit; (4) a flag só liga em produção com OK explícito do time. Consultar a skill `pipeelo-admin-change-control` antes de executar o lado ADM.
</objective>

<context>
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/.planning/phases/07-setupper-studio/07-RESEARCH.md
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/contracts/src/onboarding-payload.ts
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/complete-onboarding.ts
@C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/lib/onboarding/agent/prompt-generator.ts
@C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/lib/onboarding/v2/respostas-slice.ts

<interfaces>
```ts
// contracts/src/flow-spec.ts
export const FlowDecisionSpecSchema = z.object({ ponto: z.string(), escolha: z.string() });
export const FlowAlteracaoSchema = z.object({
  tipo: z.enum(['adicionar', 'remover', 'modificar']),
  referencia: z.string().optional(),
  descricao: z.string(),
});
export const FlowSpecSchema = z.object({
  flow_id: z.string(),
  departamento: z.string(),
  titulo: z.string(),
  origem: z.enum(['catalogo', 'custom']),
  decisoes: z.array(FlowDecisionSpecSchema),
  passos_final: z.array(z.string()),        // pode ser [] em fluxo legado (só texto)
  fluxo_texto: z.string(),                  // o detalhe humano (sempre presente — fallback p/ legado)
  alteracoes: z.array(FlowAlteracaoSchema).optional(),
});
export const StyleSpecSchema = z.object({
  confirmado: z.boolean(),
  decisoes: z.record(z.string()),           // {tom_registro, emojis, apresentacao, tratamento, jargao, confirmacao_acoes, despedida, nome_assistente?}
});
// onboarding-payload.ts (aditivo): flows: z.array(FlowSpecSchema).optional(), style: StyleSpecSchema.optional()
```
Mapeamento FlowSpec→Caso (a INSTRUÇÃO ao subagente, não código): cada FlowSpec de origem catalogo corresponde a um Caso EXISTENTE no prompt-espelho do mesmo tema (o subagente localiza pelo tema/título) — ele APLICA as decisões e passos_final ao Caso espelhado preservando slugs de função/roteamento do espelho; origem custom vira Caso NOVO ao final da SEÇÃO de casos, numeração contínua, usando APENAS funções/slugs que já existem no espelho. Style vira ajustes na seção de Estilo de escrita.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (ONB): schemas no contracts + sender</name>
  <files>contracts/src/flow-spec.ts, contracts/src/flow-spec.test.ts, contracts/src/onboarding-payload.ts, api/complete-onboarding.ts</files>
  <behavior>
    - Schemas conforme <interfaces>; payload ganha os 2 campos opcionais (v1 mantido — teste: payload SEM flows/style continua parse ok; COM eles idem).
    - `api/complete-onboarding.ts`: ao montar o payload, buscar insights `categoria='fluxo_confirmado'` da sessão; mapear → FlowSpec (payload JSONB → decisoes/passos_final/alteracoes; detalhe → fluxo_texto; origem por prefixo custom_). O fluxo `estilo_comunicacao` NÃO entra em flows[] — vira `style` (decisões efetivas; confirmado=true). Sem fluxos confirmados → campos omitidos (payload idêntico ao atual — retrocompat total).
    - Rebuild do pacote contracts conforme o setup existente (ver package.json do contracts).
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (ADM): receiver + persistência</name>
  <files>admin-pipeelo/app/api/clients/onboarding/create/route.ts</files>
  <behavior>
    - Atualizar o schema espelho do receiver (repo admin tem cópia própria do contrato — localizar como a Phase 02 resolveu o import cross-repo; seguir o MESMO mecanismo) aceitando flows/style opcionais.
    - Persistir flows/style no MESMO lugar onde agent_insights são guardados hoje (verificar no route/DB onde `agent_insights` do payload aterrissa; flows/style seguem junto — ex.: coluna JSONB da sessão/registro de onboarding no Supabase do admin). NÃO criar consumo novo aqui — só garantir que chegam e ficam disponíveis pro processor.
    - Teste: payload com flows/style → 200 e dados persistidos; sem eles → comportamento idêntico ao atual.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (ADM): flow-spec.ts — render + verificação + flag</name>
  <files>admin-pipeelo/lib/onboarding/v2/flow-spec.ts, admin-pipeelo/lib/onboarding/v2/flow-spec.test.ts</files>
  <behavior>
    - `flowspecEnabled(): boolean` — `process.env.ONBOARDING_FLOWSPEC === 'on'` lido NA CHAMADA.
    - `flowSpecsForTarget(flows: FlowSpec[], targetSlug: string): FlowSpec[]` — mapeia departamento→target conversacional (sac_geral→principal; suporte→suporte; financeiro→financeiro; vendas→vendas; closer não recebe flows). Exportar o mapa como constante.
    - `renderFlowSpecsForPrompt(specs, style): string` — bloco `<fluxos_confirmados_pelo_cliente>` com, por spec: título, decisões (bullet `ponto: escolha`), passos numerados (ou fluxo_texto), alterações; e bloco `<estilo_confirmado>` com as decisões de estilo. Termina com as INSTRUÇÕES do mapeamento FlowSpec→Caso (texto exato do <interfaces>).
    - `verifyFlowPresence(promptGerado: string, specs: FlowSpec[]): { missing: string[] }` — heurística determinística: para cada spec, o prompt contém um header de Caso (`/##?\s*Caso\s+\d+/i` — espelhar CASE_HEADER_RE de conventions.ts) cujo título compartilha ≥2 palavras-chave (>4 chars, sem stopwords) com o titulo do spec, OU contém o flow_id literal. Retorna os ausentes.
    - Testes: render contém decisões e instruções; verify acha caso presente por título parcial; verify aponta ausente; flag off/on.
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 4 (ADM): fiação no prompt-generator + report no run</name>
  <files>admin-pipeelo/lib/onboarding/agent/prompt-generator.ts</files>
  <behavior>
    - Com `flowspecEnabled()` e specs disponíveis pro target: anexar `renderFlowSpecsForPrompt(...)` ao user prompt do subagente (junto de refContent+respostas — localizar o ponto de montagem em `prompt-generator.ts:133-150`). Descobrir o caminho dos dados: o processor carrega a sessão/payload — passar flows/style por `manifest_context` ou parâmetro novo (seguir o caminho de MENOR mudança; documentar a escolha no SUMMARY).
    - Pós-geração: rodar `verifyFlowPresence`; missing → registrar no resultado do step (flags/warnings do run — mesmo canal onde os gates atuais reportam) SEM falhar o run (gate soft; vira retry-feedback via previousErrors se o mecanismo já suportar).
    - Flag off → ZERO mudança de comportamento (guard clause no topo).
  </behavior>
</task>

<task type="checkpoint" tdd="false">
  <name>Task 5 (humano): replay + revisão + decisão de ligar</name>
  <files>—</files>
  <behavior>
    - Executor PARA aqui e reporta. Humano: (1) rodar uma sessão de teste completa no estúdio (ou replay de sessão real se houver harness) com ONBOARDING_FLOWSPEC=on em dev/staging; (2) diff dos prompts gerados vs baseline sem flag; (3) validar que cada fluxo confirmado virou Caso coerente e que slugs/estrutura do espelho foram preservados; (4) decidir ligar em produção. Registrar a evidência no 07-05-SUMMARY.md.
  </behavior>
</task>

</tasks>

<verification>
- Suites dos DOIS repos verdes (`npm test` no ONB; `npx vitest run` allowlist no ADM — conferir vitest.config.ts allowlist para os testes novos RODAREM, gotcha conhecido da casa).
- Com flag off: replay/teste de onboarding produz prompts byte-idênticos ao baseline.
</verification>
