---
phase: 07-setupper-studio
plan: 05
type: execute
wave: 5
depends_on: [07-01]
files_modified:
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/contracts/src/flow-spec.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/contracts/src/flow-spec.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/contracts/src/onboarding-payload.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/complete-onboarding.ts
  - C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/supabase/migrations/20260707_onboarding_flowspec.sql
  - C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/app/api/clients/onboarding/create/route.ts
  - C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/lib/onboarding/v2/flow-spec.ts
  - C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/lib/onboarding/v2/flow-spec.test.ts
  - C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/lib/onboarding/v2/processor.ts
  - C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/lib/onboarding/v2/types.ts
  - C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/lib/onboarding/agent/generation-engine.ts
  - C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/lib/onboarding/agent/prompt-generator.ts
autonomous: false

must_haves:
  truths:
    - "FlowSpecSchema/StyleSpecSchema vivem em contracts/ (ONB, fonte de verdade) e o admin usa um ESPELHO vendorizado em lib/onboarding/v2/flow-spec.ts com comentário apontando a fonte (o admin NÃO depende do pacote contracts — decisão documentada abaixo)"
    - "complete-onboarding (ONB) monta flows[]/style/final_map a partir dos insights e envia; payload continua retrocompatível (campos aditivos opcionais)"
    - "O receiver do admin PERSISTE agent_insights, flows, style e final_map em colunas novas de onboarding_sessions (hoje ele os DESCARTA — premissa corrigida via inspeção de 2026-07-07)"
    - "Com ONBOARDING_FLOWSPEC=off (default) o pipeline do admin é byte-idêntico ao atual; com on, o prompt do subagente inclui os FlowSpecs do target e o pós-geração verifica presença (warning em flags, não falha)"
    - "Session replay/teste real evidencia a diferença antes de ligar a flag em produção"
  artifacts:
    - path: "contracts/src/flow-spec.ts"
      provides: "FlowSpecSchema + StyleSpecSchema + tipos (fonte de verdade)"
      contains: "FlowSpecSchema"
    - path: "admin-pipeelo/supabase/migrations/20260707_onboarding_flowspec.sql"
      provides: "colunas agent_insights/flows/style/final_map em onboarding_sessions (admin)"
    - path: "admin-pipeelo/lib/onboarding/v2/flow-spec.ts"
      provides: "schemas-espelho + flowspecEnabled + flowSpecsForTarget + renderFlowSpecsForPrompt + verifyFlowPresence"
  key_links:
    - from: "api/complete-onboarding.ts"
      to: "contracts/src/flow-spec.ts"
      via: "FlowSpecSchema no OnboardingPayloadSchema"
      pattern: "flows"
    - from: "admin-pipeelo/lib/onboarding/v2/processor.ts"
      to: "admin-pipeelo/lib/onboarding/v2/flow-spec.ts"
      via: "payload.flow_specs = flowSpecsForTarget(session.flows, slug) no enqueue"
      pattern: "flowSpecsForTarget"
    - from: "admin-pipeelo/lib/onboarding/agent/generation-engine.ts"
      to: "admin-pipeelo/lib/onboarding/agent/prompt-generator.ts"
      via: "flowSpecs/styleSpec threaded no runPromptGenerator + verifyFlowPresence no pós-geração"
      pattern: "verifyFlowPresence"
---

<objective>
Fechar o ciclo: o que o cliente confirmou no estúdio vira contrato estruturado que o gerador de prompts do admin materializa como Casos de Atendimento — rastreável e verificável.

⚠️ CHANGE CONTROL: este plano toca o gerador de prompts do admin (afeta TODO onboarding futuro). `autonomous: false` — o lado ADM exige: (1) flag OFF por default; (2) replay/teste real como evidência; (3) revisão humana do diff ANTES de commit; (4) a flag só liga em produção com OK explícito do time. Consultar a skill `pipeelo-admin-change-control` antes de executar o lado ADM.
</objective>

<context>
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/.planning/phases/07-setupper-studio/07-RESEARCH.md
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/contracts/src/onboarding-payload.ts
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/complete-onboarding.ts
@C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/app/api/clients/onboarding/create/route.ts
@C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/lib/onboarding/v2/processor.ts
@C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/lib/onboarding/agent/generation-engine.ts
@C:/Users/André/Documents/Desenvolvimento/admin-pipeelo/lib/onboarding/agent/prompt-generator.ts

<interfaces>
```ts
// contracts/src/flow-spec.ts (FONTE DE VERDADE — ONB)
export const FlowDecisionSpecSchema = z.object({ ponto: z.string(), escolha: z.string() });
export const FlowAlteracaoSchema = z.object({
  tipo: z.enum(['adicionar', 'remover', 'modificar']),
  referencia: z.string().optional(),
  descricao: z.string(),
});
export const FlowSpecSchema = z.object({
  flow_id: z.string(),
  departamento: z.string(),
  titulo: z.string(),
  origem: z.enum(['catalogo', 'custom']),
  decisoes: z.array(FlowDecisionSpecSchema),
  passos_final: z.array(z.string()),        // pode ser [] em fluxo legado (só texto)
  fluxo_texto: z.string(),                  // o detalhe humano (sempre presente — fallback p/ legado)
  alteracoes: z.array(FlowAlteracaoSchema).optional(),
});
export const StyleSpecSchema = z.object({
  confirmado: z.boolean(),
  decisoes: z.record(z.string()),
});
// onboarding-payload.ts (aditivo): flows?, style?, final_map?: z.string()
```

**FIAÇÃO REAL DO ADMIN (inspecionada em 2026-07-07 — o executor NÃO precisa redescobrir nada):**
```
webhook POST /api/clients/onboarding/create (route.ts:25-158)
  → upsert na tabela ADMIN `onboarding_sessions` por session_id
    colunas hoje: session_id, empresa_nome, ceo_email, access_token,
    responsaveis, datas_conclusao, respostas JSONB, status
    ❗ agent_insights/flows/style são IGNORADOS hoje (route lê só session+respostas)
    ❗ NÃO há Zod nem payload_version neste receiver (o lado-admin da Phase 02
       do repo ONB nunca chegou a este main — não seguir aquele precedente)

processor.ts (`processSession`)
  → session.respostas vem da linha acima (tipo local com `respostas` em types.ts /
    processor.ts:44 — adicionar `flows?: FlowSpec[]; style?: StyleSpec | null`)
  → enqueue de steps de geração:
      KBs:     processor.ts:793  payload.respostas_slice = buildRespostasSlice(...)
      prompts: processor.ts:838  payload.respostas_slice = buildRespostasSlice(...)
    → ADICIONAR no enqueue de PROMPTS (:838) — e SÓ nele, KBs não recebem flows:
      payload.flow_specs = flowspecEnabled() ? flowSpecsForTarget(session.flows ?? [], a.slug) : []
      payload.style_spec = flowspecEnabled() ? (session.style ?? null) : null

generation-engine.ts (`processStep`, :107-175)
  → lê payload.respostas_slice (:128-130) e chama runPromptGenerator (:138-148)
  → THREADING: passar flowSpecs: payload.flow_specs ?? [], styleSpec: payload.style_spec
  → pós-geração: após validatePrompt ok (:150), rodar verifyFlowPresence(gen.promptContent,
    payload.flow_specs); missing → anexar em `flags` no markCompleted (:164-169 — canal
    de flags JÁ EXISTE e é persistido no step; warning, NUNCA falha)

prompt-generator.ts (`runPromptGenerator`, :133-150)
  → input ganha flowSpecs?/styleSpec? ; quando presentes e não-vazios, anexar
    renderFlowSpecsForPrompt(flowSpecs, styleSpec) ao USER prompt (junto de
    refContent+respostas); system prompt inalterado
```

**Decisão cross-repo (fecha o ponto que estava "seguir a Phase 02"):** o admin NÃO importa o pacote `contracts` (não há mecanismo instalado; o deploy Easypanel/Docker não consome pacote local do outro repo). O admin usa um **espelho vendorizado** dos schemas em `lib/onboarding/v2/flow-spec.ts`, com header: `// ESPELHO de onboarding-pipeelo/contracts/src/flow-spec.ts — mudou lá, mude aqui (teste de shape cobre drift básico)`. Aceita-se o custo de sincronia manual; o receiver usa `safeParse` TOLERANTE (campo inválido → loga warning e persiste NULL naquele campo, nunca 400 — flows/style são opcionais e não podem derrubar o webhook principal).

**Mapa dept→target do gerador:** sac_geral→`principal`; suporte→`suporte`; financeiro→`financeiro`; vendas→`vendas`; closer NÃO recebe flows. Fluxo `estilo_comunicacao` não vira FlowSpec (vira style) e o style vai para TODOS os targets conversacionais.

**Instruções de mapeamento FlowSpec→Caso (texto que `renderFlowSpecsForPrompt` emite ao final dos blocos):** cada FlowSpec de origem `catalogo` corresponde a um Caso EXISTENTE no prompt-espelho do mesmo tema — localize-o pelo tema/título e APLIQUE as decisões e passos_final preservando slugs de função/roteamento do espelho; origem `custom` vira Caso NOVO ao final da seção de casos, numeração contínua, usando APENAS funções/slugs que já existem no espelho; style ajusta a seção de Estilo de escrita.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (ONB): schemas no contracts + sender</name>
  <files>contracts/src/flow-spec.ts, contracts/src/flow-spec.test.ts, contracts/src/onboarding-payload.ts, api/complete-onboarding.ts</files>
  <behavior>
    - Schemas conforme <interfaces>; payload ganha `flows?`, `style?`, `final_map?` (retrocompat: payload SEM eles continua parse ok).
    - `api/complete-onboarding.ts`: buscar insights `categoria='fluxo_confirmado'` da sessão; mapear → FlowSpec (payload JSONB → decisoes/passos_final/alteracoes; detalhe → fluxo_texto; origem por prefixo custom_). `estilo_comunicacao` NÃO entra em flows[] — vira `style`. Insight `mapa_final` (07-07) → `final_map`. Sem fluxos confirmados → campos omitidos (payload idêntico ao atual).
    - Rebuild do pacote contracts conforme o setup existente.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (ADM): migration + receiver persiste os campos novos</name>
  <files>admin-pipeelo/supabase/migrations/20260707_onboarding_flowspec.sql, admin-pipeelo/app/api/clients/onboarding/create/route.ts</files>
  <behavior>
    - Migration (aditiva): `ALTER TABLE onboarding_sessions ADD COLUMN IF NOT EXISTS agent_insights JSONB, ADD COLUMN IF NOT EXISTS flows JSONB, ADD COLUMN IF NOT EXISTS style JSONB, ADD COLUMN IF NOT EXISTS final_map TEXT;` (aplicar via MCP supabase do admin, padrão da casa).
    - route.ts: estender a interface local do payload com `agent_insights?, flows?, style?, final_map?`; validar flows/style com o safeParse TOLERANTE do espelho (warning+NULL em inválido, nunca 400); incluir os 4 campos NO INSERT (:117-128) E NO UPDATE (:82-91) — o receiver é upsert e o update de hoje esqueceria os campos.
    - Teste (padrão de teste de route do repo admin): payload com flows/style/final_map → 200/201 e colunas persistidas; sem eles → comportamento idêntico ao atual; flows malformado → 201 com flows NULL e warning logado.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (ADM): flow-spec.ts — espelho + render + verificação + flag</name>
  <files>admin-pipeelo/lib/onboarding/v2/flow-spec.ts, admin-pipeelo/lib/onboarding/v2/flow-spec.test.ts</files>
  <behavior>
    - Schemas-espelho (header de proveniência conforme <interfaces>), `flowspecEnabled()` (`ONBOARDING_FLOWSPEC === 'on'` lido NA CHAMADA), `flowSpecsForTarget` (mapa dept→target do <interfaces>, exportado como constante), `renderFlowSpecsForPrompt(specs, style)` (blocos `<fluxos_confirmados_pelo_cliente>` + `<estilo_confirmado>` + as instruções de mapeamento EXATAS do <interfaces>), `verifyFlowPresence(promptGerado, specs)` (header de Caso via regex espelhando `CASE_HEADER_RE` de `lib/prompt-optimizer/agent/blueprint/conventions.ts:49` + título com ≥2 keywords em comum (>4 chars, sem stopwords) OU flow_id literal → presente; retorna `{missing: string[]}`).
    - Testes: render contém decisões/passos/instruções; verify acha Caso por título parcial; verify aponta ausente; flag off/on; **conferir que os testes novos RODAM (allowlist do vitest.config.ts do admin — gotcha conhecido da casa)**.
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 4 (ADM): fiação processor → engine → generator (caminho exato do <interfaces>)</name>
  <files>admin-pipeelo/lib/onboarding/v2/processor.ts, admin-pipeelo/lib/onboarding/v2/types.ts, admin-pipeelo/lib/onboarding/agent/generation-engine.ts, admin-pipeelo/lib/onboarding/agent/prompt-generator.ts</files>
  <behavior>
    - types.ts: tipo da sessão ganha `flows?: FlowSpec[] | null; style?: StyleSpec | null` (import do espelho); tipo do payload de step (`types.ts:95`) ganha `flow_specs?: FlowSpec[]; style_spec?: StyleSpec | null`.
    - processor.ts: no enqueue de PROMPTS (`:838`) adicionar `flow_specs`/`style_spec` conforme <interfaces> (guard `flowspecEnabled()`); conferir que a query que carrega a sessão traz as colunas novas (se for `select("*")`, nada a fazer; se select explícito, adicionar).
    - generation-engine.ts: threading no `runPromptGenerator` (`:138-148`) + `verifyFlowPresence` após `validatePrompt` ok (`:150`) com missing anexado ao objeto do `markCompleted` (`:164-169`) como `flags: [...(gen.flags ?? []), ...missing.map(m => \`flowspec_ausente:\${m}\`)]`.
    - prompt-generator.ts: input estendido; com specs presentes, `renderFlowSpecsForPrompt` anexado ao user prompt na montagem (`:133-150`); flag off ou specs vazios → prompt byte-idêntico ao atual (guard clause).
  </behavior>
</task>

<task type="checkpoint" tdd="false">
  <name>Task 5 (humano): replay + revisão + decisão de ligar</name>
  <files>—</files>
  <behavior>
    - Executor PARA aqui e reporta. Humano: (1) rodar uma sessão de teste completa no estúdio com ONBOARDING_FLOWSPEC=on em dev/staging; (2) diff dos prompts gerados vs baseline sem flag; (3) validar que cada fluxo confirmado virou Caso coerente com slugs/estrutura do espelho preservados; (4) aplicar a migration no Supabase de produção do admin (via MCP); (5) decidir ligar em produção. Registrar a evidência no 07-05-SUMMARY.md.
  </behavior>
</task>

</tasks>

<verification>
- Suites dos DOIS repos verdes (`npm test` no ONB; vitest allowlist no ADM).
- Com flag off: teste de onboarding produz prompts byte-idênticos ao baseline.
- Grep sanity no ADM: `flowSpecsForTarget` em processor.ts; `verifyFlowPresence` em generation-engine.ts; receiver persiste os 4 campos no INSERT e no UPDATE.
</verification>
