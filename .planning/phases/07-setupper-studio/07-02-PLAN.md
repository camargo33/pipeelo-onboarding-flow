---
phase: 07-setupper-studio
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/agent/[action].ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/agent/_state.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/agent/_state.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/src/lib/preview/types.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/src/lib/preview/scripts.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/src/lib/preview/scripts.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/src/components/preview/SmartphoneFrame.tsx
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/src/components/preview/PreviewConversation.tsx
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/src/pages/OnboardingChat.tsx
autonomous: true

must_haves:
  truths:
    - "GET /api/agent/state devolve o estado de preview (empresa, estilo efetivo com padrões aplicados, fluxos com status/decisões/passos) autenticado por slug+token"
    - "Cada um dos 13 fluxos do catálogo tem um builder de roteiro que reflete decisões confirmadas; fluxo não confirmado renderiza com os padrões e badge 'padrão sugerido'"
    - "O estilo efetivo altera o roteiro de TODOS os fluxos (saudação, emoji, tratamento, nome do assistente)"
    - "OnboardingChat em desktop mostra chat à esquerda e smartphone à direita; após tool_result de save_answers/confirm_flow o preview atualiza sozinho"
    - "Fluxo custom_<slug> confirmado renderiza os passos_final como roteiro narrado"
  artifacts:
    - path: "api/agent/_state.ts"
      provides: "handler GET state — projeção do estado da sessão para o preview"
    - path: "src/lib/preview/scripts.ts"
      provides: "buildPreviewScript(flowId, state) + builders por fluxo"
      contains: "buildPreviewScript"
    - path: "src/components/preview/SmartphoneFrame.tsx"
      provides: "moldura WhatsApp-like tema-consciente"
  key_links:
    - from: "src/pages/OnboardingChat.tsx"
      to: "api/agent/_state.ts"
      via: "fetch GET /api/agent/state após tool_result relevante"
      pattern: "/api/agent/state"
    - from: "src/components/preview/PreviewConversation.tsx"
      to: "src/lib/preview/scripts.ts"
      via: "import { buildPreviewScript }"
      pattern: "buildPreviewScript"
---

<objective>
O smartphone: preview determinístico da conversa do cliente final, dirigido pelo estado configurado, atualizando ao vivo a cada decisão. Zero LLM neste plano.
</objective>

<context>
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/.planning/phases/07-setupper-studio/07-RESEARCH.md
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/agent/_history.ts
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/flows.ts
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/src/pages/OnboardingChat.tsx

<interfaces>
```ts
// Contrato do GET /api/agent/state?slug&token — shape EXATO (o front tipa a partir dele)
interface PreviewState {
  empresa: { nome: string; nomeAssistente: string | null };   // nomeAssistente: decisão nome_assistente do estilo, ou null
  departamentoAtual: string;                                   // primeiro dept não-concluído na DEPARTMENT_ORDER do modo
  estilo: {                                                    // SEMPRE completo: decisão confirmada OU padrão do catálogo
    confirmado: boolean;                                       // o fluxo estilo_comunicacao foi confirmado?
    decisoes: Record<string, string>;                          // {tom_registro: 'caloroso e próximo', emojis: 'nunca', ...}
  };
  fluxos: Array<{
    flowId: string;
    departamento: string;
    titulo: string;
    status: 'confirmado' | 'pendente';
    decisoes: Record<string, string>;                          // confirmadas, ou padrões do catálogo p/ pendente
    passosFinal: string[];                                     // payload JSONB se confirmado; fluxoPadrao do catálogo se pendente
    custom: boolean;                                           // flow_id começa com custom_
  }>;
  respostasPreview: {                                          // subset de respostas que os roteiros usam (slots)
    itensMenu?: string[];                                      // derivado da decisão itens_menu do saudacao_menu, se personalizado
    horarios?: Record<string, string>;                         // respostas horario_atendimento resumidas por dept (texto pronto "seg-sex 8h-18h")
    planos?: Array<{ nome: string; preco?: string; velocidade?: string }>;  // do repeater planos_internet_residencial, cap 3
  };
}

// Roteiro de preview
type PreviewMessage = { from: 'cliente' | 'ia'; text: string };
```
Construção do estilo efetivo: começar dos `padrao` das decisões do STYLE_FLOW; sobrepor payload JSONB se confirmado. Mesmo merge para decisões de fluxos pendentes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: endpoint GET /api/agent/state</name>
  <files>api/agent/_state.ts, api/agent/_state.test.ts, api/agent/[action].ts</files>
  <behavior>
    - Registrar `state` no router `[action].ts` (mesmo padrão de `history`).
    - Auth via `assertSessionAccess` (igual `_history.ts`); métodos ≠ GET → 405.
    - Carrega em paralelo: sessão, `onboarding_respostas`, insights `categoria='fluxo_confirmado'` (com `payload`).
    - Monta `PreviewState` conforme a interface acima. Regras: nomeAssistente = decisão `nome_assistente` do estilo SE confirmada e não for a opção 'nome da empresa' (nesse caso usar empresa.nome); horários derivados de respostas `horario_atendimento`/`horario_semanal` quando existirem (formatador simples: "seg–sex HH:MM–HH:MM; sáb ..."; dia `nao_atende` omitido); planos do repeater `planos_internet_residencial` (cap 3, campos nome/preco/velocidade se presentes).
    - Fluxos: TODOS os do catálogo do modo da sessão (modo comercial → só sac_geral? NÃO: modo comercial exige identificacao+vendas → fluxos de vendas apenas + estilo NÃO se aplica; nesse caso incluir só fluxos de vendas) + quaisquer `custom_*` confirmados (payload → passosFinal; sem payload → detalhe splitado por linhas numeradas como fallback).
    - Testes (mock supabase como nos testes existentes do repo): sessão sem nada confirmado → estilo.confirmado=false com padrões completos e 13 fluxos pendente; fluxo confirmado com payload → status confirmado + passosFinal do payload; custom confirmado aparece com custom=true; token errado → 401.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2: builders de roteiro (scripts.ts)</name>
  <files>src/lib/preview/types.ts, src/lib/preview/scripts.ts, src/lib/preview/scripts.test.ts</files>
  <behavior>
    - `buildPreviewScript(flowId: string, state: PreviewState): PreviewMessage[]` — dispatch por flowId com builder específico para os 13 fluxos do catálogo + fallback custom/desconhecido.
    - **Helpers de estilo (usados por TODOS os builders)** — é isto que faz o preview "mostrar na prática":
      - `greeting(state)`: saudação de abertura obedecendo `apresentacao` (com/sem "sou a <nome>, assistente..."), `nome_assistente`, `tom_registro` (sóbrio: "Olá. Como posso ajudar?"; caloroso: "Oi! Que bom te ver por aqui 😊 como posso ajudar?" — o 😊 SÓ se `emojis` ≠ 'nunca'), empresa.nome.
      - `addressCustomer(state, nome='João')`: 'primeiro nome' → "João"; 'Sr./Sra.' → "Sr. João".
      - `maybeEmoji(state, dept, emoji)`: retorna emoji se política permite naquele dept, senão ''.
    - **Builders por fluxo (roteiros de 4-8 balões, decisão-sensíveis).** Especificação exata dos ramos que CADA builder deve honrar:
      - `estilo_comunicacao`: mini-demo de 4 balões (cliente diz "oi", IA sauda com o estilo; cliente pergunta algo técnico "minha ONU tá piscando LOS", IA responde traduzindo ou não conforme `jargao`).
      - `saudacao_menu`: saudação (helper) → triagem conforme decisão `triagem` (ramo 'direto ao assunto' pula a pergunta "já é cliente?") → menu com `respostasPreview.itensMenu` ?? padrão Suporte/Financeiro/Comercial/Outros.
      - `identificacao_cliente`: pede o dado da decisão `dados_coleta`; confirma titular conforme `confirma_titular`; se `momento_endereco`='logo após identificar', mostra a pergunta de endereço multi-contrato.
      - `roteamento_intencoes`: cliente pede "2ª via"; IA responde roteando e narra `[transfere para o time Financeiro]` como linha de sistema (from 'ia', prefixo "— " itálico via marcador `*...*`).
      - `cancelamento_retencao`: cliente pede cancelamento por lentidão; roteiro segue o braço da decisão `motivo_lentidao` (diagnóstico primeiro vs oferta) e `ponto_humano`.
      - `fora_horario_transferencia`: mensagem fora do horário com o horário REAL de `respostasPreview.horarios` quando existir ("Nosso time do suporte volta seg–sex às 8h") e o template da decisão `msg_fora_horario`.
      - `encerramento_nps`: "posso ajudar em algo mais?" → nota → ramo `nota_alta` (link Google se escolhido).
      - `diagnostico_suporte`: consulta proativa conforme `status_proativo`; bloqueio financeiro conforme `bloqueio_primeiro`; foto conforme `quando_foto`.
      - `apresentacao_planos`: usa `respostasPreview.planos` reais quando existirem (senão placeholders "Plano Fibra 500 Mega — R$ 99,90"); quantidade conforme `quantas_opcoes`; benefícios conforme `beneficios_quando`.
      - `qualificacao_pre_cadastro`, `viabilidade_sem_cobertura`, `segunda_via_cobranca`: idem, ramos das respectivas decisões (`formato_envio` muda o balão da fatura: "PIX + boleto juntos" vs pergunta de preferência).
      - Fallback custom: `passosFinal.map(p => ({from:'ia', text: `*${p}*`}))` precedido de greeting — roteiro narrado.
    - Determinístico puro (sem Date.now/random) — testável por snapshot.
    - Testes mínimos: (1) greeting muda com tom_registro e apresentacao; (2) emojis 'nunca' → nenhum emoji em NENHUM builder (varre todos com regex emoji); (3) cancelamento com 'suporte diagnostica primeiro' não contém oferta de plano; (4) apresentacao_planos com 2 planos reais mostra os nomes reais; (5) custom fallback renderiza os passos.
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 3: componentes do smartphone</name>
  <files>src/components/preview/SmartphoneFrame.tsx, src/components/preview/PreviewConversation.tsx</files>
  <behavior>
    - `SmartphoneFrame`: moldura de celular (borda arredondada 2xl, notch discreto, aspect ~9:19, max-w-[380px]) com header estilo app de mensagens: avatar circular (inicial do nome do assistente), nome (nomeAssistente ?? empresa.nome), status "online". Tema: usar tokens tailwind existentes do projeto (dark mode class-based já ativo — `index.html` tem class="dark"); NÃO hardcodear verde WhatsApp (evitar trade dress) — usar a paleta pipeelo (`--pipeelo-green` etc. de tailwind.config.ts).
    - `PreviewConversation({ flow, state })`: renderiza `buildPreviewScript` como balões (cliente à direita, IA à esquerda — espelhando um app de mensagens do ponto de vista do CLIENTE FINAL... atenção: convenção de apps: MINHAS mensagens à direita; aqui o "eu" é o cliente final); texto entre `*...*` renderiza como linha de sistema (itálico, centralizado, cinza — as narrações de transferência). Balões com animação de entrada escalonada (stagger ~80ms, framer-motion já é dep? verificar package.json; se não, CSS animation simples).
    - Badge de status no topo da conversa: fluxo `confirmado` → chip verde "Fluxo confirmado ✓"; `pendente` → chip âmbar "Padrão sugerido — vocês ainda vão configurar".
    - Seletor de fluxo: dropdown compacto (shadcn Select, já no repo) listando os fluxos do departamento atual + confirmados de outros; agrupado por departamento.
    - Rodapé FIXO: "Simulação de referência — o texto final é gerado na configuração da conta." (fonte xs, muted).
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 4: layout split + auto-atualização no OnboardingChat</name>
  <files>src/pages/OnboardingChat.tsx</files>
  <behavior>
    - Desktop (≥lg): grid `lg:grid-cols-[minmax(0,1fr)_400px]` — chat à esquerda, smartphone à direita (sticky top). Mobile: chat full + botão flutuante "Ver como fica 📱" abrindo o preview como sheet/drawer (shadcn Sheet já no repo? verificar; senão modal simples).
    - Estado novo: `previewState: PreviewState | null` + `activeFlowId: string | null`. Carregar state junto do load inicial (adicionar ao `Promise.all` existente, tolerante a erro como o history).
    - Auto-refresh: preferir o evento SSE `state_changed` (07-HARNESS-SPEC §7, implementado no 07-04) → refetch `/api/agent/state` (debounce 500ms). Se o 07-04 ainda não tiver sido executado quando este plano rodar, usar interim o `tool_result` de `save_answers`|`confirm_flow`|`complete_department` com um `TODO(07-04): migrar para state_changed`.
    - Auto-follow: ao receber `tool_result` de `confirm_flow` com `fluxo_confirmado` → `setActiveFlowId(<o fluxo>)` + micro-highlight no smartphone (badge pulsa 1x). Default de activeFlowId: primeiro fluxo pendente do departamento atual.
    - NÃO tocar na lógica de envio/stream existente (`OnboardingChat.tsx:167-219`) além do hook de refetch.
  </behavior>
</task>

</tasks>

<verification>
- `npm test` verde. `npm run build` verde.
- Manual (dev): abrir sessão de teste `/:slug/ia`, confirmar um fluxo pelo chat e ver o smartphone trocar para ele com as decisões aplicadas; responder horário no questionário e ver o roteiro de fora-de-horário citar o horário real.
</verification>
