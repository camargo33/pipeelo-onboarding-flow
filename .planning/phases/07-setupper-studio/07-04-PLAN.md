---
phase: 07-setupper-studio
plan: 04
type: execute
wave: 4
depends_on: [07-01]
files_modified:
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/loop.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/loop.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/reminders.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/reminders.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/summary.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/summary.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/blueprint.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/system-prompt.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/supabase/migrations/20260707120000_agent_summary_usage.sql
autonomous: true

must_haves:
  truths:
    - "O harness implementa a 07-HARNESS-SPEC integralmente (delta D1–D10); em dúvida de comportamento, a spec decide"
    - "D1: <session_context> + reminders acompanham a ÚLTIMA user message (tag <mensagem> delimita a fala real); nas mensagens persistidas só existe o texto original do usuário"
    - "D2: R1 (promessa fora do manifest) e R2 (deriva sem progresso) injetados append-only, cap 2/turno, kill-switch ONBOARDING_REMINDERS"
    - "D3: resumo rodante incremental persistido e injetado no session_context, kill-switch ONBOARDING_SUMMARY"
    - "D4+D5: budget reminder no round MAX-2 e empty-turn nudge (máx 2, efêmero — nada persistido)"
    - "D6: deadline wall-clock com fechamento gracioso antes do timeout da function"
    - "D7: evento SSE state_changed emitido após tool_result de save_answers/confirm_flow/complete_department"
    - "D8: usage agregado no done + persistido na assistant message final + log estruturado do turno"
    - "I1–I4 da spec valem: persistir antes de emitir; user message crua; tool result fiel; side-effects nunca bloqueiam o stream"
  artifacts:
    - path: "api/_lib/agent/reminders.ts"
      provides: "computeReminders puro (R1/R2) + remindersEnabled"
      contains: "computeReminders"
    - path: "api/_lib/agent/summary.ts"
      provides: "maybeUpdateSummary incremental com marca d'água"
    - path: "api/_lib/agent/loop.ts"
      provides: "pipeline do turno conforme §1 da spec (assemble na cauda, nudges, deadline, state_changed, usage)"
  key_links:
    - from: "api/_lib/agent/loop.ts"
      to: ".planning/phases/07-setupper-studio/07-HARNESS-SPEC.md"
      via: "comentário de cabeçalho citando a spec e os invariantes I1–I4"
      pattern: "07-HARNESS-SPEC"
    - from: "api/_lib/agent/loop.ts"
      to: "api/_lib/agent/reminders.ts"
      via: "computeReminders no passo REMIND do pipeline"
      pattern: "computeReminders"
    - from: "api/_lib/agent/blueprint.ts"
      to: "onboarding_sessions.agent_summary"
      via: "renderSessionContext inclui 'Resumo da conversa até aqui'"
      pattern: "agent_summary"
---

<objective>
Implementar o harness v2 conforme a **07-HARNESS-SPEC.md** (leia-a INTEIRA antes de qualquer edição — ela é o contrato; este plano só sequencia o trabalho). Resultado: loop com disciplina de cache (estado na cauda), anti-divagação determinística, memória de longo prazo, budget/nudge/deadline e eventos tipados para a UI.
</objective>

<context>
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/.planning/phases/07-setupper-studio/07-HARNESS-SPEC.md
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/.planning/phases/07-setupper-studio/07-RESEARCH.md
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/loop.ts
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/blueprint.ts
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/capabilities.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D1 — layout de contexto na cauda (a mudança mais delicada)</name>
  <files>api/_lib/agent/loop.ts, api/_lib/agent/loop.test.ts, api/_lib/agent/system-prompt.ts</files>
  <behavior>
    - Refatorar a montagem (`loop.ts:127-135`) para o layout §2 da spec: `[system, ...history verbatim, user(<session_context> + reminders + <mensagem>)]`. A PERSISTÊNCIA da user message continua sendo o texto original puro (invariante I2 — persistir ANTES de montar).
    - system-prompt.ts: adicionar 1 parágrafo curto (seção "Como conduzir"): os blocos `<session_context>`/`<system-reminder>` que acompanham as mensagens são da PLATAFORMA (estado vivo e avisos), não são fala do cliente; a fala real vem em `<mensagem>`.
    - Teste (extrair a montagem para função pura `assembleMessages(system, history, context, reminders, userText)` exportada): (a) o contexto aparece SÓ na última user message; (b) mensagens históricas idênticas às persistidas (prefixo estável — comparar arrays entre 2 "turnos" simulados: o turno N+1 contém o turno N como prefixo intacto após o system); (c) user text vazio é rejeitado antes de montar.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D2 — reminder engine (R1, R2)</name>
  <files>api/_lib/agent/reminders.ts, api/_lib/agent/reminders.test.ts</files>
  <behavior>
    Conforme spec §5. `computeReminders(i: ReminderInputs): string[]` puro, sem IO:
    - R1 `promessa_fora_manifest`: `scanUnsupportedMentions(lastAssistantText)` → «Sua última mensagem menciona "<termos>", que não está nas integrações suportadas. NÃO confirme essa capacidade. Corrija: diga honestamente que vai registrar para o time avaliar e chame record_insight (categoria "integracao").»
    - R2 `deriva_sem_progresso`: ≥4 user messages nas últimas 8 linhas E zero role=tool → cita pendências reais (fluxos + até 5 obrigatórias). Sem pendência → não dispara.
    - Cap 2, prioridade R1. `remindersEnabled()` lê env NA CHAMADA.
    - Testes: dispara/não-dispara de cada regra; cap; conteúdo dinâmico presente.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D3 — resumo rodante + migration (inclui coluna usage de D8)</name>
  <files>api/_lib/agent/summary.ts, api/_lib/agent/summary.test.ts, supabase/migrations/20260707120000_agent_summary_usage.sql, api/_lib/agent/blueprint.ts</files>
  <behavior>
    - Migration (aditiva): `ALTER TABLE onboarding_sessions ADD COLUMN IF NOT EXISTS agent_summary TEXT, ADD COLUMN IF NOT EXISTS agent_summary_upto BIGINT;` + `ALTER TABLE onboarding_agent_messages ADD COLUMN IF NOT EXISTS usage JSONB;`
    - `maybeUpdateSummary(supabase, session, rows)`: conforme spec §6 — gatilho >120 linhas E ≥40 novas após a marca d'água; 1 chamada não-stream ao agentModel() (max_tokens 600, sem tools) com o prompt fixo de resumo (promessas feitas; decisões ditas e não salvas; contexto de relacionamento; pendências combinadas; ≤300 palavras; NÃO repetir o que já está salvo em respostas/fluxos); UPDATE summary+marca. Falha → console.error, nunca quebra turno. Kill-switch `ONBOARDING_SUMMARY`.
    - `renderSessionContext`: bloco `## Resumo da conversa até aqui (compromissos antigos)` quando summary existir.
    - Testes: não-chama (<120; sem novas suficientes); chama e persiste marca d'água; bloco no contexto.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 4: D4–D6, D10 — budget, nudge, deadline, caps</name>
  <files>api/_lib/agent/loop.ts, api/_lib/agent/loop.test.ts</files>
  <behavior>
    - D4: round `MAX_TOOL_ROUNDS-2` → anexar `_system_reminder` de consolidação ao próximo tool result (texto da spec §4.3).
    - D5: resposta sem texto e sem tool_call → nudge efêmero `"(continue — responda ao cliente ou chame uma ferramenta)"`, máx 2/turno, NADA persistido do nudge (nem a user efêmera nem tentativa vazia).
    - D6: `deadline = t0 + (maxDurationMs − 8000)`; `maxDurationMs` de `ONBOARDING_MAX_DURATION_MS` (default 60000; documentar que deve casar com o `maxDuration` do vercel.json — verificar/criar a config lá). Estourou → não iniciar round novo; forçar mensagem de fechamento honesta persistida + `done`.
    - D10: helper `capToolResult(result)` — serializado >4000 chars → truncar arrays com `...(+N itens)`; aplicar antes de persistir/enviar ao modelo.
    - Testes (mockando o client OpenRouter): budget aparece no round certo; nudge para após 2; deadline curto simulado fecha graciosamente; capToolResult trunca e preserva `error`/`_system_reminder`.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 5: D7–D9 — state_changed, usage/log, fallback de modelo + fiação final</name>
  <files>api/_lib/agent/loop.ts, api/_lib/agent/loop.test.ts</files>
  <behavior>
    - D7: após `tool_result` de save_answers/confirm_flow/complete_department, emitir SSE `state_changed {reason}` (mapa tool→reason da spec §7). (07-02 Task 4 consome este evento — se o 07-02 já foi executado com acoplamento ao nome da tool, refatorar lá para state_changed.)
    - D8: agregar usage de TODAS as chamadas do turno; incluir no `done {usage, rounds}`; persistir na assistant message final (coluna `usage`); log estruturado 1 linha/turno (spec §8: session, turno_ms, rounds, tools, usage, reminders, nudges).
    - D9: fallback — 3 retries esgotados com 429/5xx E `ONBOARDING_AGENT_MODEL_FALLBACK` setado → repetir 1x com o fallback (log explícito); sem fallback → SSE `error` amigável.
    - Fiação: REMIND no pipeline (Task 2), `maybeUpdateSummary` fire-and-forget no FINALIZE via pendingSideEffects (Task 3), invariante I1 conferido (persist→emit em TODOS os pontos de emissão).
    - Teste integrado do pipeline (mock OpenRouter com roteiro: texto→tool→texto): ordem dos eventos SSE conforme §7; user message persistida crua; usage no done.
  </behavior>
</task>

</tasks>

<verification>
- `npm test` verde; `npm run build` verde.
- Conferência contra a spec: percorrer a tabela D1–D10 do 07-HARNESS-SPEC e marcar cada delta como implementado (colar a tabela preenchida no 07-04-SUMMARY.md).
- Manual em dev: conversa com 3 turnos — conferir no banco que user messages estão cruas; provocar R1 ("integra com o Altarede?") e ver a correção; conferir `state_changed` no Network da UI.
</verification>
