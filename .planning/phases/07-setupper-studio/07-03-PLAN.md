---
phase: 07-setupper-studio
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/preview/compiler.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/preview/compiler.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/agent/_preview.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/agent/_preview.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/agent/[action].ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/supabase/migrations/20260707110000_preview_budget.sql
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/src/components/preview/PreviewConversation.tsx
autonomous: true

must_haves:
  truths:
    - "POST /api/agent/preview-chat responde como a IA de atendimento usando um draft-prompt compilado do estado atual, no modelo ONBOARDING_PREVIEW_MODEL (default openai/gpt-5-mini, reasoning low)"
    - "ONBOARDING_PREVIEW=off → 403 estruturado e a UI esconde o modo test-drive"
    - "Sessão que estourou ONBOARDING_PREVIEW_MAX_MSGS recebe 429 estruturado com mensagem amigável"
    - "O draft-prompt contém: identidade+estilo efetivo, fluxos confirmados como Casos numerados, regras anti-invenção, e NUNCA credenciais/respostas sensíveis (SENSITIVE_PREFIXES)"
    - "Test-drive é stateless server-side: o histórico do preview vem do cliente (cap 20 msgs, 2k chars cada)"
  artifacts:
    - path: "api/_lib/preview/compiler.ts"
      provides: "compileDraftPrompt(state) — draft fiel à ESTRUTURA dos prompts de produção"
      contains: "compileDraftPrompt"
    - path: "api/agent/_preview.ts"
      provides: "handler preview-chat com flag + budget + SSE"
  key_links:
    - from: "api/agent/_preview.ts"
      to: "api/_lib/preview/compiler.ts"
      via: "import { compileDraftPrompt }"
      pattern: "compileDraftPrompt"
    - from: "api/agent/_preview.ts"
      to: "api/_lib/agent/openrouter.ts"
      via: "mesmo cliente de streaming do _chat"
      pattern: "openrouter"
---

<objective>
Test-drive: o cliente digita NO smartphone como se fosse o cliente final e recebe respostas do rascunho da IA dele — com o mesmo modelo classe-produção dos tenants (gpt-5-mini low). É a prova viva de "como vai ficar", limitada e atrás de kill-switch.
</objective>

<context>
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/.planning/phases/07-setupper-studio/07-RESEARCH.md
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/agent/_chat.ts
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/openrouter.ts
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/agent/_state.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compileDraftPrompt</name>
  <files>api/_lib/preview/compiler.ts, api/_lib/preview/compiler.test.ts</files>
  <behavior>
    - `compileDraftPrompt(state: PreviewState & { insightsPublicos?: string[] }): string` — módulo puro.
    - Estrutura ESPELHANDO a dos prompts de produção (RESEARCH §2 — SEÇÕES), simplificada:
      1. `# Identidade` — nome do assistente, empresa, papel (atendimento WhatsApp de provedor).
      2. `# Estilo de escrita` — REGRAS derivadas das decisões de estilo efetivas (tom, emoji por setor, tratamento, jargão→sempre traduzir gera "explique termos técnicos em linguagem simples (ONU→equipamento, LOS→sinal)", apresentacao, despedida, confirmacao_acoes). Mensagens curtas, 1 pergunta por vez, formato mobile (patterns P8/P9/P16 — incluir SEMPRE, não são opcionais).
      3. `# Casos de Atendimento` — para CADA fluxo confirmado: `## Caso N: <titulo>` + passos numerados de passosFinal + linha `Decisões aplicadas:` com as decisões. Fluxos NÃO confirmados do departamento atual entram com os padrões e a nota `(configuração padrão — em ajuste)`.
      4. `# Regras duras` — NUNCA inventar valores/planos/status (se não há dado, dizer que vai verificar e oferecer transferência); NUNCA citar nomes internos de sistemas/funções; quando o assunto não casa nenhum Caso → informar educadamente e reoferecer o menu; responder SEMPRE PT-BR.
      5. `# Simulação` — declara que é um PREVIEW sem acesso a sistemas: quando um passo exigiria consultar o ERP, responda com dado FICTÍCIO PLAUSÍVEL e marque com "(exemplo)" — ex.: "sua fatura de julho (exemplo): R$ 99,90". [Racional: sem tools no preview; marcar exemplos evita o cliente achar que a IA inventará valores em produção.]
    - Filtro de segurança: aceitar APENAS os campos de `PreviewState` (que já exclui respostas sensíveis por construção) — teste garante que o output NUNCA contém strings de perguntas com prefixos `SENSITIVE_PREFIXES` (importar a lista de tools.ts ou duplicar com comentário de espelho).
    - Determinístico; testes: contém "## Caso 1" quando há 1 fluxo confirmado; estilo sóbrio+sem emoji gera regra explícita "não use emojis"; nunca contém "erp_"/"gateway_".
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2: endpoint preview-chat + budget + migration</name>
  <files>api/agent/_preview.ts, api/agent/_preview.test.ts, api/agent/[action].ts, supabase/migrations/20260707110000_preview_budget.sql</files>
  <behavior>
    - Migration: `ALTER TABLE onboarding_sessions ADD COLUMN IF NOT EXISTS preview_msgs_used INTEGER NOT NULL DEFAULT 0;`
    - Registrar `preview-chat` no router. POST only. Body Zod: `{ slug, token, flow_id?: string, messages: Array<{role:'user'|'assistant', content: string}> }` — messages 1..20, content ≤2000 chars (400 se violar).
    - Gates em ordem: flag `ONBOARDING_PREVIEW !== 'off'` senão 403 `{error:'preview_disabled'}`; auth `assertSessionAccess`; budget: `preview_msgs_used >= Number(ONBOARDING_PREVIEW_MAX_MSGS ?? 40)` → 429 `{error:'preview_budget_exceeded', message:'Limite de mensagens de teste desta sessão atingido...'}`.
    - Monta estado via a MESMA função de projeção do `_state.ts` (extrair a montagem do PreviewState para `api/_lib/preview/state.ts` se necessário — refactor permitido, manter _state.ts como wrapper fino) → `compileDraftPrompt` → chama OpenRouter streaming com `model: ONBOARDING_PREVIEW_MODEL ?? 'openai/gpt-5-mini'`, `reasoning: {effort:'low'}`, `temperature 0.4`, `max_tokens 1024`, SEM tools.
    - Responde SSE com os mesmos eventos `text|done|error` do _chat (reusar o writer). Após `done`, incrementa `preview_msgs_used` (+1 por request).
    - Testes: flag off → 403; budget estourado → 429; body inválido → 400; happy path mockando openrouter → SSE text+done e incremento persistido.
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 3: modo test-drive na UI</name>
  <files>src/components/preview/PreviewConversation.tsx</files>
  <behavior>
    - Toggle no smartphone: "Roteiro" (default, preview determinístico do 07-02) ↔ "Testar ao vivo" (test-drive). O toggle "Testar ao vivo" só aparece se o state indicar preview habilitado (adicionar `previewEnabled: boolean` ao PreviewState no _state.ts, lido da env server-side).
    - Modo test-drive: input de mensagem no rodapé do smartphone; histórico local (useState, zerado ao trocar de fluxo ou clicar "recomeçar"); envia `{messages}` completo ao endpoint; stream na bolha da IA (mesmo parser SSE do chat principal — extrair helper se fácil, senão duplicar 30 linhas com comentário).
    - Banner no topo do modo: "Você está falando com o RASCUNHO da sua IA — dados são exemplos." Erro 429 → banner amigável e input desabilitado.
    - Botão "recomeçar" limpa o histórico local.
  </behavior>
</task>

</tasks>

<verification>
- `npm test` + `npm run build` verdes.
- Manual: confirmar 2 fluxos numa sessão dev, entrar no test-drive, pedir "quero a segunda via" e verificar que a resposta segue o fluxo confirmado (formato de envio escolhido) e marca valores com "(exemplo)".
</verification>
