---
phase: 07-setupper-studio
plan: 01
type: execute
wave: 1
depends_on: [07-00]
files_modified:
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/capabilities.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/capabilities.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/flows.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/flows.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/tools.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/tools.test.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/system-prompt.ts
  - C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/supabase/migrations/20260707100000_agent_flow_payload.sql
autonomous: true

must_haves:
  truths:
    - "CAPABILITY_MANIFEST é a única fonte do 'o que a plataforma suporta' — o system prompt gera essa seção do manifest, não de texto hardcoded"
    - "Existe o fluxo estilo_comunicacao no catálogo, departamento sac_geral, com ≥7 pontos de decisão de forma/tom"
    - "confirm_flow aceita passos_final: string[] (min 3) e alteracoes[], grava payload JSONB, e continua aceitando fluxo_final string (deprecated) de sessões antigas"
    - "confirm_flow com passo citando ERP fora do manifest retorna erro instrutivo (não salva)"
    - "Migration adiciona onboarding_agent_insights.payload JSONB NULL — aditiva, sem tocar linhas existentes"
  artifacts:
    - path: "api/_lib/agent/capabilities.ts"
      provides: "CAPABILITY_MANIFEST + helpers findCapability/renderCapabilitiesSection/scanUnsupportedMentions"
      contains: "CAPABILITY_MANIFEST"
    - path: "api/_lib/agent/flows.ts"
      provides: "STYLE_FLOW estilo_comunicacao + decisões novas em fluxos existentes"
      contains: "estilo_comunicacao"
    - path: "supabase/migrations/20260707100000_agent_flow_payload.sql"
      provides: "coluna payload JSONB em onboarding_agent_insights"
  key_links:
    - from: "api/_lib/agent/system-prompt.ts"
      to: "api/_lib/agent/capabilities.ts"
      via: "import { renderCapabilitiesSection }"
      pattern: "renderCapabilitiesSection"
    - from: "api/_lib/agent/tools.ts"
      to: "api/_lib/agent/capabilities.ts"
      via: "import { scanUnsupportedMentions } em confirmFlow"
      pattern: "scanUnsupportedMentions"
---

<objective>
Fundação de dados da fase: (1) capability manifest como fonte única; (2) eixos de personalização que faltam viram fluxo `estilo_comunicacao` + decisões novas nos fluxos existentes; (3) `confirm_flow` v2 estruturado com persistência JSONB.

Output: catálogo cobre os 10 eixos de personalização do RESEARCH §3; confirm_flow produz dados compiláveis (passos endereçáveis); manifest pronto para reminders (07-04) e draft-prompt (07-03).
</objective>

<context>
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/.planning/phases/07-setupper-studio/07-RESEARCH.md
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/flows.ts
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/tools.ts
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/api/_lib/agent/system-prompt.ts
@C:/Users/André/Documents/Desenvolvimento/onboarding-pipeelo/supabase/migrations/20260702100000_agent_insights_flow_id.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: capabilities.ts — manifest + render + scanner</name>
  <files>api/_lib/agent/capabilities.ts, api/_lib/agent/capabilities.test.ts</files>
  <behavior>
    - `CAPABILITY_MANIFEST.erps` contém exatamente os ERPs suportados com aliases: ixc (["ixc","ixcsoft"]), mk_solutions (["mk","mk solutions","mk-auth"? NÃO — mk-auth é outro produto, não incluir]), voalle, sgp, hubsoft, rbx, topsapp (["topp sap","topsapp"]) — mesma lista de `system-prompt.ts:59` hoje. Cada um: `{ id, nome, aliases: string[] }`.
    - `CAPABILITY_MANIFEST.features`: lista `{ id, nome, descricao, representacao }` onde representacao ∈ 'prompt_caso' | 'config_plataforma' | 'followup' | 'kb' — cobrindo: roteamento entre agentes IA, transferência para fila humana, horários por fila + plantão, 2ª via/PIX/linha digitável, desbloqueio de confiança, status de conexão, abertura de OS, viabilidade por endereço, follow-ups automáticos, tags/motivos, NPS, KBs consultadas em tempo real. (Transcrever de `system-prompt.ts:57-63` — NÃO inventar features novas.)
    - `CAPABILITY_MANIFEST.limites`: array de strings — o que NÃO dá pra prometer (transcrever de `system-prompt.ts:64`).
    - `renderCapabilitiesSection(): string` — gera o markdown da seção "# O que a plataforma Pipeelo suporta" EQUIVALENTE em conteúdo à atual (`system-prompt.ts:55-64`); teste compara que todos os ERPs e features aparecem no texto.
    - `scanUnsupportedMentions(texto: string): string[]` — detector LÉXICO simples: retorna menções a ERPs/sistemas NÃO suportados. Implementação: lista `KNOWN_UNSUPPORTED = ['altarede', 'isp integrator', 'gerencianet erp', ...]` (deixar vazia inicialmente é aceitável) + heurística: padrão `/integra(?:ção|r)? com o? ?([A-Z][\w-]{2,})/gi` cujo grupo capturado não casa alias de ERP suportado nem feature. Case-insensitive. Retorna lista de termos (dedupe). Testes: "integração com o Altarede" → ['Altarede']; "integração com o IXC" → []; texto sem menção → [].
    - Sem dependência de rede/DB — módulo puro (importável pelo frontend se preciso).
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2: flows.ts — STYLE_FLOW + decisões novas + catálogo unificado</name>
  <files>api/_lib/agent/flows.ts, api/_lib/agent/flows.test.ts</files>
  <behavior>
    - Novo `StandardFlow` com `id: 'estilo_comunicacao'`, `departamento: 'sac_geral'`, `titulo: 'Estilo de comunicação da IA'`, objetivo explicando que define a VOZ da IA em todos os departamentos. `fluxoPadrao`: descrição do estilo default (profissional-caloroso, sem emoji, trata pelo primeiro nome, traduz termos técnicos, não se apresenta como "assistente virtual", confirma antes de ações de efeito).
    - Decisões do estilo (ids EXATOS — o preview 07-02 e o compiler 07-03 dependem deles):
      1. `nome_assistente` — "Qual o nome da IA de atendimento? (ex.: Ana, Léo — ou o nome da empresa)" opcoes: ['nome próprio (coletar)', 'nome da empresa'], sem padrao.
      2. `tom_registro` — opcoes: ['sóbrio e direto', 'caloroso e próximo', 'neutro profissional'], padrao 'neutro profissional'.
      3. `emojis` — opcoes: ['nunca', 'moderado (só em vendas/comercial)', 'em todos os setores'], padrao 'nunca'.
      4. `apresentacao` — "A IA se apresenta como assistente virtual ou conversa como a equipe?" opcoes: ['conversa como a equipe (recomendado)', 'se apresenta como assistente na 1ª mensagem'], padrao 'conversa como a equipe (recomendado)'. (Base: pattern P-naturalize "auto-declaração assistente virtual".)
      5. `tratamento` — opcoes: ['primeiro nome', 'Sr./Sra. + sobrenome'], padrao 'primeiro nome'.
      6. `jargao` — "Termos técnicos (ONU, PON, LOS): traduzir sempre pra linguagem leiga?" opcoes: ['sempre traduzir', 'pode usar termo técnico com explicação'], padrao 'sempre traduzir'.
      7. `confirmacao_acoes` — "Antes de ações de efeito (reiniciar equipamento, trocar senha): confirmar sempre?" opcoes: ['sempre confirmar', 'executar direto quando o pedido for explícito'], padrao 'sempre confirmar'.
      8. `despedida` — opcoes: ['padrão (agradece + se coloca à disposição)', 'texto do cliente (coletar)'], padrao 'padrão (agradece + se coloca à disposição)'.
    - Decisões NOVAS em fluxos existentes (eixo 6 do RESEARCH — coleta de dados): em `identificacao_cliente`, adicionar decisão `dados_coleta` — "Que dado a IA pede pra localizar o cadastro quando o telefone não resolve?" opcoes: ['CPF/CNPJ', 'nome completo + endereço', 'código do cliente'], padrao 'CPF/CNPJ'.
    - `STANDARD_FLOWS` passa a incluir `estilo_comunicacao` como PRIMEIRO fluxo de sac_geral (ordem importa: estilo cedo alimenta o preview de tudo).
    - Novo export `STYLE_FLOW_ID = 'estilo_comunicacao'` e helper `styleDecisions(confirmedInsights): Record<string,string> | null` — extrai as decisões do payload JSONB do fluxo de estilo confirmado (null se não confirmado; consumidor usa padrões).
    - Testes: catálogo contém estilo_comunicacao com os 8 ids de decisão; flowsForDepartment('sac_geral')[0].id === 'estilo_comunicacao'; identificacao_cliente contém dados_coleta; NENHUM id de fluxo/decisão PREEXISTENTE foi alterado (snapshot dos ids: os 12 flow ids atuais e seus decision ids exatamente como em flows.ts hoje — regressão de compat com sessões em andamento).
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 3: migration payload JSONB + confirm_flow v2</name>
  <files>supabase/migrations/20260707100000_agent_flow_payload.sql, api/_lib/agent/tools.ts, api/_lib/agent/tools.test.ts</files>
  <behavior>
    Migration (aditiva, padrão de `20260702100000_agent_insights_flow_id.sql`):
    - `ALTER TABLE onboarding_agent_insights ADD COLUMN IF NOT EXISTS payload JSONB;`
    - Comentário na coluna: estrutura `{passos_final: string[], decisoes: {ponto,escolha}[], alteracoes?: {tipo,referencia,descricao}[]}`.

    confirm_flow v2 (em tools.ts):
    - Schema da tool ganha: `passos_final: { type: 'array', items: {type:'string'}, description: 'Passo a passo FINAL numerável, um passo por item, já com as decisões do cliente aplicadas' }` e `alteracoes: { type:'array', items: {type:'object', properties:{ tipo:{enum:['adicionar','remover','modificar']}, referencia:{type:'string', description:'passo do fluxo padrão afetado (texto ou número)'}, descricao:{type:'string'} }, required:['tipo','descricao'] } }` (alteracoes opcional). `fluxo_final` continua no schema com description marcando DEPRECATED (usar passos_final).
    - Validação em `confirmFlow()`: aceita passos_final (≥3 itens, cada um ≥10 chars trim) OU fluxo_final legado (≥80 chars). Se ambos ausentes/curtos → erro instrutivo pedindo passos_final.
    - Validação manifest: concatenar passos_final+fluxo_final e rodar `scanUnsupportedMentions`; se retornar termos → `{ error: 'O fluxo menciona integração/sistema fora do suportado: <termos>. Ajuste o passo ou registre um insight categoria "integracao" e mantenha o fluxo dentro do que a plataforma faz.' }` (NÃO salva).
    - Persistência: além do `detalhe` texto atual (`tools.ts:422-427` — manter formato, derivando de passos_final quando presente: passos numerados + bloco DECISÕES), grava `payload: { passos_final, decisoes, alteracoes }` no insert (`tools.ts:435-442`).
    - Retrocompat: chamada SÓ com fluxo_final (formato antigo) continua funcionando — payload gravado com `passos_final: []` e o texto no detalhe (teste explícito).
    - Testes novos (manter TODOS os existentes verdes): v2 feliz (passos_final + decisões completas → ok, payload gravado); passos_final com 2 itens → erro; menção a sistema não suportado → erro com o termo; legado fluxo_final ≥80 → ok com aviso `warnings: ['use passos_final...']`.
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 4: system-prompt.ts — seção de capabilities gerada + metodologia do estilo</name>
  <files>api/_lib/agent/system-prompt.ts</files>
  <behavior>
    - Substituir o bloco hardcoded "# O que a plataforma Pipeelo suporta" (`system-prompt.ts:55-64`) por interpolação de `renderCapabilitiesSection()` (manter o prompt ESTÁVEL: a função é determinística, o texto resultante deve ser byte-idêntico entre chamadas).
    - Acrescentar à seção da metodologia (`system-prompt.ts:31-45`) 2 parágrafos: (a) o fluxo `estilo_comunicacao` é o PRIMEIRO a montar no sac_geral — as decisões de estilo valem para TODOS os departamentos e alimentam o preview que o cliente vê ao lado do chat; (b) mencionar que o cliente VÊ um smartphone com a simulação — quando ele reagir ao preview ("não gostei dessa mensagem"), trate como decisão de fluxo/estilo: localize o fluxo correspondente, ajuste a decisão e reconfirme via confirm_flow (nunca prometa mudança sem registrá-la em decisão/insight).
    - Atualizar a linha do confirm_flow na seção de disciplina de ferramentas: citar passos_final como formato preferido.
    - NÃO mudar mais nada do prompt (estabilidade = cache).
  </behavior>
</task>

</tasks>

<verification>
- `npm test` verde (suite existente + novos).
- Grep sanity: `renderCapabilitiesSection` importado em system-prompt.ts; `scanUnsupportedMentions` importado em tools.ts; nenhuma ocorrência restante da lista de ERPs hardcoded no system-prompt.
- Migration aplicável: `supabase db push` em dev (ou aplicar via MCP) sem erro; INSERT antigo (sem payload) continua funcionando.
</verification>
