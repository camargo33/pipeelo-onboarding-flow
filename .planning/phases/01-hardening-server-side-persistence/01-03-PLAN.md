---
phase: 01-hardening-server-side-persistence
plan: 03
type: execute
wave: 2
depends_on: ["01-00", "01-01", "01-02"]
files_modified:
  - src/lib/api-client.ts
  - src/lib/debounced-save.ts
  - src/components/TurnstileWidget.tsx
  - src/hooks/useOnboarding.ts
  - src/pages/Onboarding.tsx
  - src/pages/OnboardingSession.tsx
  - src/pages/NovoOnboarding.tsx
  - src/lib/api-client.test.ts
  - src/lib/debounced-save.test.ts
  - src/components/TurnstileWidget.test.tsx
  - package.json
autonomous: true
requirements: [HARD-01, HARD-02, HARD-07]
must_haves:
  truths:
    - "Zero `supabase.from('onboarding_sessions'|'onboarding_respostas')` em src/ fora de src/integrations/ (audit script HARD-01 verde)"
    - "Páginas Onboarding/OnboardingSession/NovoOnboarding consomem APIs via fetch wrappers"
    - "Per-question autosave debounced 500ms salva sem bloquear input (HARD-02)"
    - "TurnstileWidget renderiza captcha + propaga token via callback (HARD-07 client-side)"
    - "Magic link `?token=...` é honrado: Onboarding.tsx lê token da URL e usa em get/save"
  artifacts:
    - path: "src/lib/api-client.ts"
      provides: "Wrappers tipados pra /api/sessions/* (sessionApi.create/get/saveResposta/completeDepartment)"
      exports: ["sessionApi", "ApiError"]
    - path: "src/lib/debounced-save.ts"
      provides: "useDebouncedAutosave hook + flushAutosaves para beforeunload"
      exports: ["useDebouncedAutosave"]
    - path: "src/components/TurnstileWidget.tsx"
      provides: "Wrapper React do Cloudflare Turnstile"
      exports: ["TurnstileWidget"]
    - path: "src/pages/Onboarding.tsx"
      provides: "Page refatorada — autosave per-question + remove fire-and-forget legacy"
    - path: "src/pages/OnboardingSession.tsx"
      provides: "Page refatorada — gate via api-client + progress 5/5 (preview, fix completo no Plan 04)"
    - path: "src/pages/NovoOnboarding.tsx"
      provides: "Page refatorada — TurnstileWidget + chama /api/sessions/create"
  key_links:
    - from: "src/pages/Onboarding.tsx"
      to: "/api/sessions/save-resposta"
      via: "sessionApi.saveResposta em useDebouncedAutosave"
      pattern: "sessionApi\\.saveResposta"
    - from: "src/pages/Onboarding.tsx"
      to: "URL ?token="
      via: "useSearchParams"
      pattern: "searchParams\\.get\\(['\\\"]token['\\\"]\\)"
    - from: "src/pages/NovoOnboarding.tsx"
      to: "TurnstileWidget"
      via: "<TurnstileWidget onSuccess={setTurnstileToken} />"
      pattern: "<TurnstileWidget"
---

<objective>
Migrar TODOS os call sites do front que usam `supabase.from('onboarding_*')` para os endpoints `/api/sessions/*` criados em Plan 01-01. Adicionar autosave debounced + Turnstile widget. Fechar gate de auditoria HARD-01.

Purpose: Esta é a wave que torna RLS lockdown (Plan 05) seguro. SEM esta migração, lock = quebra prod imediata.
Output: 3 pages refatoradas + 3 utilitários novos (api-client, useDebouncedAutosave, TurnstileWidget) + audit script verde.
</objective>

<execution_context>
@C:/Users/dopeb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dopeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-hardening-server-side-persistence/01-RESEARCH.md
@.planning/phases/01-hardening-server-side-persistence/01-01-SUMMARY.md
@.planning/phases/01-hardening-server-side-persistence/01-VALIDATION.md
@src/pages/Onboarding.tsx
@src/pages/OnboardingSession.tsx
@src/pages/NovoOnboarding.tsx
@src/hooks/useOnboarding.ts
@src/integrations/supabase/client.ts
@src/types/onboarding.ts

<interfaces>
<!-- Endpoints prontos do Plan 01-01 -->

POST /api/sessions/create
  body { empresa_nome, cnpj, turnstileToken } → 201 { slug, access_token } | 400 | 409 cnpj_already_exists

GET /api/sessions/get?slug=&token=
  → 200 { session, respostas[] } | 401 invalid_session | 410 session_expired

PUT /api/sessions/save-resposta
  body { slug, token, departamento, pergunta_id, valor } → 200 { ok, saved_at } | 401 | 400

POST /api/sessions/complete-department
  body { slug, token, departamento, responsavel_nome } → 200 { ok } | 403 identification_gate

POST /api/sessions/send-magic-link
  body { slug } → 200 { ok }

<!-- Tipos do projeto -->
From src/types/onboarding.ts:
```typescript
export const DEPARTMENT_ORDER: DepartmentId[] = ['identificacao','sac_geral','financeiro','suporte','vendas'];
export type DepartmentId = typeof DEPARTMENT_ORDER[number];
```

<!-- Cloudflare Turnstile lib -->
Recomendação: usar wrapper oficial-ish `@marsidev/react-turnstile` (mais maduro), fallback para `<script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer>` + global render API.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: api-client + useDebouncedAutosave + TurnstileWidget (utils Wave 2)</name>
  <files>src/lib/api-client.ts, src/lib/debounced-save.ts, src/components/TurnstileWidget.tsx, src/lib/api-client.test.ts, src/lib/debounced-save.test.ts, src/components/TurnstileWidget.test.tsx, package.json</files>
  <behavior>
    - api-client: fetch wrappers tipados (`sessionApi.get/create/saveResposta/completeDepartment`); `keepalive: true`; throw ApiError com status + code
    - useDebouncedAutosave: aceita value+saver+delay; cancela timer em re-input; cleanup em unmount; sendBeacon fallback em pagehide
    - TurnstileWidget: renderiza Turnstile com siteKey; chama onSuccess(token); refresh-expired="auto"
  </behavior>
  <action>
    1. Instalar:
    ```bash
    npm install @marsidev/react-turnstile
    ```

    2. `src/lib/api-client.ts`:
    ```typescript
    export class ApiError extends Error {
      constructor(public status: number, message: string, public code?: string) {
        super(message);
      }
    }

    async function api<T>(path: string, init?: RequestInit): Promise<T> {
      const r = await fetch(path, {
        ...init,
        headers: { 'Content-Type': 'application/json', ...(init?.headers ?? {}) },
        keepalive: true, // CRÍTICO: autosave + tab close
      });
      if (!r.ok) {
        const body = await r.json().catch(() => ({}));
        throw new ApiError(r.status, body.error ?? r.statusText, body.code);
      }
      return r.json() as Promise<T>;
    }

    export type SessionDTO = {
      id: string; slug: string;
      empresa_nome: string;
      status_identificacao: string;
      status_sac_geral: string;
      status_financeiro: string;
      status_suporte: string;
      status_vendas: string;
      [k: string]: unknown;
    };
    export type RespostaDTO = { departamento: string; pergunta_id: string; valor: unknown; updated_at: string };

    export const sessionApi = {
      create: (input: { empresa_nome: string; cnpj: string; turnstileToken: string }) =>
        api<{ slug: string; access_token: string }>('/api/sessions/create', { method: 'POST', body: JSON.stringify(input) }),
      get: (slug: string, token: string) =>
        api<{ session: SessionDTO; respostas: RespostaDTO[] }>(
          `/api/sessions/get?slug=${encodeURIComponent(slug)}&token=${encodeURIComponent(token)}`
        ),
      saveResposta: (input: { slug: string; token: string; departamento: string; pergunta_id: string; valor: unknown }) =>
        api<{ ok: true; saved_at: string }>('/api/sessions/save-resposta', { method: 'PUT', body: JSON.stringify(input) }),
      completeDepartment: (input: { slug: string; token: string; departamento: string; responsavel_nome: string }) =>
        api<{ ok: true }>('/api/sessions/complete-department', { method: 'POST', body: JSON.stringify(input) }),
      sendMagicLink: (slug: string) =>
        api<{ ok: true }>('/api/sessions/send-magic-link', { method: 'POST', body: JSON.stringify({ slug }) }),
    };
    ```

    3. `src/lib/debounced-save.ts`:
    ```typescript
    import { useEffect, useRef } from 'react';

    export function useDebouncedAutosave<T>(
      value: T,
      saver: (v: T) => Promise<void>,
      delayMs = 500,
      enabled = true
    ) {
      const lastSavedRef = useRef<T | undefined>(undefined);
      const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null);
      const pendingValueRef = useRef<T | undefined>(undefined);

      useEffect(() => {
        if (!enabled) return;
        if (Object.is(value, lastSavedRef.current)) return;
        pendingValueRef.current = value;
        if (timerRef.current) clearTimeout(timerRef.current);
        timerRef.current = setTimeout(async () => {
          try {
            await saver(value);
            lastSavedRef.current = value;
            pendingValueRef.current = undefined;
          } catch (e) {
            console.error('[autosave] failed', e);
          }
        }, delayMs);
        return () => { if (timerRef.current) clearTimeout(timerRef.current); };
      }, [value, saver, delayMs, enabled]);

      // beforeunload: flush pendentes via fetch keepalive (já no api-client)
      useEffect(() => {
        const flush = () => {
          if (pendingValueRef.current !== undefined && !Object.is(pendingValueRef.current, lastSavedRef.current)) {
            // saver tem keepalive=true via api-client → request continua mesmo com tab fechando
            saver(pendingValueRef.current).catch(() => { /* best-effort */ });
          }
        };
        window.addEventListener('pagehide', flush);
        return () => window.removeEventListener('pagehide', flush);
      }, [saver]);
    }
    ```

    4. `src/components/TurnstileWidget.tsx`:
    ```tsx
    import { Turnstile } from '@marsidev/react-turnstile';

    type Props = {
      onSuccess: (token: string) => void;
      onExpire?: () => void;
      onError?: () => void;
    };

    export function TurnstileWidget({ onSuccess, onExpire, onError }: Props) {
      const siteKey = import.meta.env.VITE_TURNSTILE_SITE_KEY as string | undefined;
      if (!siteKey) {
        console.warn('[TurnstileWidget] VITE_TURNSTILE_SITE_KEY ausente — modo dev permissivo');
        return null;
      }
      return (
        <Turnstile
          siteKey={siteKey}
          onSuccess={onSuccess}
          onExpire={onExpire ?? (() => onSuccess(''))}
          onError={onError ?? (() => onSuccess(''))}
          options={{ theme: 'dark', refreshExpired: 'auto' }}
        />
      );
    }
    ```

    5. Tests:

    `src/lib/api-client.test.ts`:
    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';
    import { sessionApi, ApiError } from './api-client';

    describe('sessionApi', () => {
      beforeEach(() => { vi.restoreAllMocks(); });
      it('get monta querystring com slug+token encoded', async () => {
        const fetchSpy = vi.spyOn(globalThis, 'fetch').mockResolvedValue(
          new Response(JSON.stringify({ session: {}, respostas: [] }), { status: 200 })
        );
        await sessionApi.get('s/lug', 'to k+en');
        expect(fetchSpy).toHaveBeenCalledWith(
          expect.stringContaining('slug=s%2Flug&token=to%20k%2Ben'),
          expect.objectContaining({ keepalive: true })
        );
      });
      it('throws ApiError com status em response não-ok', async () => {
        vi.spyOn(globalThis, 'fetch').mockResolvedValue(
          new Response(JSON.stringify({ error: 'invalid_session' }), { status: 401 })
        );
        await expect(sessionApi.get('s', 't')).rejects.toMatchObject({ status: 401, message: 'invalid_session' });
      });
      it('saveResposta usa method PUT com body JSON', async () => {
        const fetchSpy = vi.spyOn(globalThis, 'fetch').mockResolvedValue(
          new Response(JSON.stringify({ ok: true, saved_at: 'now' }), { status: 200 })
        );
        await sessionApi.saveResposta({ slug: 's', token: 't', departamento: 'identificacao', pergunta_id: 'q1', valor: 'v' });
        const call = fetchSpy.mock.calls[0];
        expect(call[1]).toMatchObject({ method: 'PUT' });
        expect(JSON.parse(call[1]!.body as string)).toMatchObject({ pergunta_id: 'q1' });
      });
    });
    ```

    `src/lib/debounced-save.test.ts`:
    ```typescript
    import { describe, it, expect, vi } from 'vitest';
    import { renderHook } from '@testing-library/react';
    import { useDebouncedAutosave } from './debounced-save';

    describe('useDebouncedAutosave', () => {
      it('chama saver após delay', async () => {
        vi.useFakeTimers();
        const saver = vi.fn().mockResolvedValue(undefined);
        const { rerender } = renderHook(({ v }) => useDebouncedAutosave(v, saver, 500), { initialProps: { v: 'a' } });
        rerender({ v: 'b' });
        expect(saver).not.toHaveBeenCalled();
        await vi.advanceTimersByTimeAsync(500);
        expect(saver).toHaveBeenCalledWith('b');
        vi.useRealTimers();
      });
      it('cancela timer anterior em re-input', async () => {
        vi.useFakeTimers();
        const saver = vi.fn().mockResolvedValue(undefined);
        const { rerender } = renderHook(({ v }) => useDebouncedAutosave(v, saver, 500), { initialProps: { v: 'a' } });
        rerender({ v: 'b' });
        await vi.advanceTimersByTimeAsync(300);
        rerender({ v: 'c' });
        await vi.advanceTimersByTimeAsync(300);
        expect(saver).not.toHaveBeenCalled(); // timer reiniciado
        await vi.advanceTimersByTimeAsync(200);
        expect(saver).toHaveBeenCalledTimes(1);
        expect(saver).toHaveBeenCalledWith('c');
        vi.useRealTimers();
      });
      it('não duplica saves para mesmo valor', async () => {
        vi.useFakeTimers();
        const saver = vi.fn().mockResolvedValue(undefined);
        const { rerender } = renderHook(({ v }) => useDebouncedAutosave(v, saver, 500), { initialProps: { v: 'a' } });
        rerender({ v: 'b' });
        await vi.advanceTimersByTimeAsync(500);
        rerender({ v: 'b' }); // mesmo valor
        await vi.advanceTimersByTimeAsync(500);
        expect(saver).toHaveBeenCalledTimes(1);
        vi.useRealTimers();
      });
    });
    ```

    `src/components/TurnstileWidget.test.tsx`:
    ```tsx
    import { describe, it, expect, vi } from 'vitest';
    import { render } from '@testing-library/react';

    vi.mock('@marsidev/react-turnstile', () => ({
      Turnstile: (props: any) => <div data-testid="turnstile-mock" data-sitekey={props.siteKey} />,
    }));
    import { TurnstileWidget } from './TurnstileWidget';

    describe('<TurnstileWidget />', () => {
      it('retorna null se VITE_TURNSTILE_SITE_KEY ausente', () => {
        const original = (import.meta as any).env?.VITE_TURNSTILE_SITE_KEY;
        (import.meta as any).env.VITE_TURNSTILE_SITE_KEY = '';
        const { container } = render(<TurnstileWidget onSuccess={() => {}} />);
        expect(container.firstChild).toBeNull();
        (import.meta as any).env.VITE_TURNSTILE_SITE_KEY = original;
      });
      it('renderiza Turnstile com siteKey quando env presente', () => {
        (import.meta as any).env.VITE_TURNSTILE_SITE_KEY = 'site-key-123';
        const { getByTestId } = render(<TurnstileWidget onSuccess={() => {}} />);
        expect(getByTestId('turnstile-mock').getAttribute('data-sitekey')).toBe('site-key-123');
      });
    });
    ```
  </action>
  <verify>
    <automated>npm test -- --run src/lib/api-client.test.ts src/lib/debounced-save.test.ts src/components/TurnstileWidget.test.tsx</automated>
  </verify>
  <done>3 arquivos util + 3 testes; @marsidev/react-turnstile instalado.</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar NovoOnboarding.tsx + OnboardingSession.tsx (remove supabase.from)</name>
  <files>src/pages/NovoOnboarding.tsx, src/pages/OnboardingSession.tsx</files>
  <action>
    1. `src/pages/NovoOnboarding.tsx` — substituir `supabase.from('onboarding_sessions').insert(...)` por `sessionApi.create(...)`:

    Pattern do refactor (verificar arquivo atual primeiro):
    ```tsx
    // ANTES (típico):
    // const { data, error } = await supabase.from('onboarding_sessions').insert({...}).select().single();

    // DEPOIS:
    import { sessionApi, ApiError } from '@/lib/api-client';
    import { TurnstileWidget } from '@/components/TurnstileWidget';

    const [turnstileToken, setTurnstileToken] = useState('');

    async function handleSubmit() {
      try {
        const { slug, access_token } = await sessionApi.create({
          empresa_nome,
          cnpj: cnpj.replace(/\D/g, ''),
          turnstileToken,
        });
        // Redirect com token na URL (HARD-03 magic link)
        navigate(`/${slug}?token=${access_token}`);
      } catch (e) {
        if (e instanceof ApiError) {
          if (e.status === 409) toast.error('Já existe sessão para este CNPJ');
          else if (e.status === 403) toast.error('Captcha inválido');
          else toast.error('Erro ao criar sessão: ' + e.message);
        }
      }
    }

    // No JSX:
    <TurnstileWidget onSuccess={setTurnstileToken} />
    <Button disabled={!turnstileToken || ...}>Criar</Button>
    ```

    2. `src/pages/OnboardingSession.tsx` — substituir fetch direto Supabase por `sessionApi.get`:

    ```tsx
    // ANTES:
    // const { data: session } = await supabase.from('onboarding_sessions').select('*').eq('slug', slug).single();

    // DEPOIS:
    import { useSearchParams } from 'react-router-dom';
    import { sessionApi } from '@/lib/api-client';

    const [searchParams] = useSearchParams();
    const token = searchParams.get('token') ?? '';
    const { slug } = useParams<{ slug: string }>();

    useEffect(() => {
      if (!slug || !token) return;
      sessionApi.get(slug, token)
        .then(({ session }) => setSession(session))
        .catch((e) => {
          if (e.status === 401) toast.error('Link inválido ou expirado');
          else if (e.status === 410) toast.error('Sessão expirou (>30 dias)');
          else toast.error('Erro ao carregar sessão');
        });
    }, [slug, token]);
    ```

    Manter UI atual (gate visual em `startDepartment` continua igual; servidor reforça em Plan 04). Progress bar fix (`/4` → `/5`) fica para Plan 04 (HARD-06).

    3. Auditar: `git grep "supabase.from" src/pages/NovoOnboarding.tsx src/pages/OnboardingSession.tsx` → DEVE retornar 0 ocorrências em referência a `onboarding_sessions` ou `onboarding_respostas`. Outras tabelas (admin auth) podem permanecer.
  </action>
  <verify>
    <automated>git grep -nE "supabase\\.from\\([\\\"\\x27]onboarding_(sessions|respostas)" src/pages/NovoOnboarding.tsx src/pages/OnboardingSession.tsx; test $? -eq 1</automated>
  </verify>
  <done>2 pages migradas; zero referências a `supabase.from('onboarding_*')`; ambas usam sessionApi e respeitam ?token= da URL.</done>
</task>

<task type="auto">
  <name>Task 3: Refatorar Onboarding.tsx — autosave per-question + remove fire-and-forget legacy de save final</name>
  <files>src/pages/Onboarding.tsx, src/hooks/useOnboarding.ts</files>
  <action>
    Esta é a refatoração mais complexa do plano. Princípios:
    - Autosave per-question (HARD-02): cada change em `respostas` dispara `useDebouncedAutosave` que chama `sessionApi.saveResposta(...)`
    - Remove `supabase.from('onboarding_respostas').upsert(...)` no `handleSubmit` (server agora é fonte de verdade contínua)
    - `handleSubmit` final do depto chama `sessionApi.completeDepartment(...)` apenas para marcar status
    - Magic link: ler `?token=` via `useSearchParams`; passar para todas chamadas
    - Manter integrações pós-conclusão (`/api/provision-tenant`, `/api/sync-department`, `/api/complete-onboarding`, `/api/send-email`) — `keepalive: true` já está no api-client; legacy fetches manter por ora (Phase 2 reescreve)

    Pseudocódigo (adaptar ao código atual):
    ```tsx
    import { useSearchParams, useParams } from 'react-router-dom';
    import { sessionApi, ApiError } from '@/lib/api-client';
    import { useDebouncedAutosave } from '@/lib/debounced-save';

    export default function Onboarding() {
      const { slug, departamento } = useParams<{ slug: string; departamento: DepartmentId }>();
      const [searchParams] = useSearchParams();
      const token = searchParams.get('token') ?? '';

      const onboarding = useOnboarding(departamento!); // existing hook
      const { respostas, currentQuestion, setResposta, sections } = onboarding;

      // Hidratar do servidor ao montar
      useEffect(() => {
        if (!slug || !token || !departamento) return;
        sessionApi.get(slug, token)
          .then(({ respostas }) => {
            const filtered = respostas.filter(r => r.departamento === departamento);
            // hydrate state local
            filtered.forEach(r => setResposta(r.pergunta_id, r.valor));
          })
          .catch(e => {
            if (e.status === 401 || e.status === 410) {
              toast.error('Link inválido ou expirado');
            }
          });
      }, [slug, token, departamento]);

      // Autosave per-question: quando currentQuestion muda OU sua resposta muda
      const currentValue = currentQuestion ? respostas[currentQuestion.id] : undefined;
      const saver = useCallback(async (v: unknown) => {
        if (!slug || !token || !departamento || !currentQuestion) return;
        await sessionApi.saveResposta({
          slug, token, departamento,
          pergunta_id: currentQuestion.id,
          valor: v,
        });
      }, [slug, token, departamento, currentQuestion?.id]);
      useDebouncedAutosave(currentValue, saver, 500, !!currentQuestion);

      async function handleSubmitDepartment() {
        // Remove: upsert direto via supabase
        // Add:
        try {
          await sessionApi.completeDepartment({
            slug: slug!, token,
            departamento: departamento!,
            responsavel_nome: responsavelNome,
          });
          // Mantém side-effects legacy (Phase 2 reescreve)
          fireAndForgetIntegrations();
          showSuccessUI();
        } catch (e) {
          if (e instanceof ApiError && e.status === 403) {
            toast.error('Complete primeiro a Identificação');
            navigate(`/${slug}?token=${token}`);
          } else {
            toast.error('Erro ao concluir departamento');
          }
        }
      }
    }
    ```

    Side-effects legacy (`/api/provision-tenant`, `/api/sync-department`, `/api/complete-onboarding`, `/api/send-email`):
    - **Manter intactos** durante Phase 1
    - Adicionar `keepalive: true` aos fetches (mitigation Pitfall 1 fire-and-forget)
    - Phase 2 reescreve com outbox pattern

    `useOnboarding.ts` — não precisa mudança se já expõe `setResposta(perguntaId, valor)` e `respostas` map. Se internamente fazia `supabase.from(...)`, removê-los (estado puro local).

    Auditar:
    ```bash
    npm run audit:no-supabase-from
    # → DEVE retornar exit 0 agora
    ```
  </action>
  <verify>
    <automated>npm run audit:no-supabase-from && npm test -- --run</automated>
  </verify>
  <done>Onboarding.tsx refatorada (autosave + completeDepartment via API); audit script HARD-01 retorna exit 0; suite Vitest verde.</done>
</task>

</tasks>

<verification>
- `npm run audit:no-supabase-from` → exit 0 (HARD-01 ENFORCEMENT GATE FECHADO)
- `npm test -- --run` → todos verdes
- `npm run build` → green
- Manual smoke (recomendado antes de Wave 3): `npm run dev`, criar sessão em /novo, preencher pergunta, fechar aba, reabrir com magic link → resposta presente
</verification>

<success_criteria>
HARD-01 + HARD-02 + HARD-07 (client) complete:
- Audit script verde (zero `supabase.from(onboarding_*)` em src/)
- Autosave debounced 500ms funcional per-question
- Magic link `?token=` honrado em Onboarding e OnboardingSession
- TurnstileWidget integrado em NovoOnboarding (validação server fica para Plan 04)
- 3 pages refatoradas + 3 utils novos
</success_criteria>

<output>
Após completar, criar `.planning/phases/01-hardening-server-side-persistence/01-03-SUMMARY.md` documentando:
- Pages refatoradas + paths exatos
- Confirmação audit script HARD-01 verde
- TODOs deixados para Plan 04: validação server-side Turnstile + rate-limit + CNPJ inline + progress bar fix
</output>
