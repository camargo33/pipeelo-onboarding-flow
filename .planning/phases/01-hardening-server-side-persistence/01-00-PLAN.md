---
phase: 01-hardening-server-side-persistence
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - vitest.setup.ts
  - tests/_helpers/handler.ts
  - tests/_helpers/supabase-mock.ts
  - tests/_helpers/db-staging.ts
  - scripts/audit-no-supabase-from.mjs
  - .github/workflows/ci.yml
autonomous: true
requirements: [HARD-01]
must_haves:
  truths:
    - "Comando `npm test -- --run` executa Vitest e termina com exit 0 (suite vazia OK)"
    - "Comando `node scripts/audit-no-supabase-from.mjs` retorna exit 0 hoje (zero ocorrências em src/) e exit 1 quando alguma `supabase.from('onboarding_*')` for adicionada"
    - "CI roda audit + test em todo PR antes de merge"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Config Vitest com jsdom env + alias @/* → src/*"
      contains: "defineConfig"
    - path: "vitest.setup.ts"
      provides: "jest-dom matchers + Supabase env mocks"
      contains: "@testing-library/jest-dom"
    - path: "scripts/audit-no-supabase-from.mjs"
      provides: "Gate CI HARD-01 — fail se supabase.from(onboarding*) em src/"
      contains: "process.exit"
    - path: "tests/_helpers/handler.ts"
      provides: "Helper para invocar Vercel Functions sem `vercel dev`"
      exports: ["invokeHandler"]
    - path: "tests/_helpers/supabase-mock.ts"
      provides: "Service-role client mock para testes unit"
    - path: ".github/workflows/ci.yml"
      provides: "CI step rodando audit + npm test em PRs"
  key_links:
    - from: "package.json scripts.test"
      to: "vitest.config.ts"
      via: "vitest CLI"
      pattern: "\"test\":\\s*\"vitest"
    - from: "package.json scripts.audit:no-supabase-from"
      to: "scripts/audit-no-supabase-from.mjs"
      via: "node CLI"
      pattern: "audit-no-supabase-from"
---

<objective>
Estabelecer infraestrutura de testes (Vitest 2.x + helpers + jsdom) e gate CI de auditoria HARD-01 antes de qualquer task de feature. Wave 0 BLOCKING — todos os planos seguintes dependem dos test stubs e helpers criados aqui.

Purpose: Projeto tem zero testes hoje. Migrate-then-lock (Pitfall 4) exige automação confiável antes de tocar RLS. Audit script é gate inegociável de HARD-01.
Output: Vitest config, helpers, audit script, CI workflow, package.json scripts, test stubs vazios para todos os arquivos referenciados na VALIDATION.md.
</objective>

<execution_context>
@C:/Users/dopeb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dopeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-hardening-server-side-persistence/01-RESEARCH.md
@.planning/phases/01-hardening-server-side-persistence/01-VALIDATION.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONCERNS.md
@package.json
@vite.config.ts
@tsconfig.app.json

<interfaces>
<!-- Existing exports Wave 0 must integrate with -->

From api/_lib/supabase.ts:
```typescript
export function getServiceSupabase(): SupabaseClient<Database>;
```

From src/integrations/supabase/client.ts:
```typescript
export const supabase: SupabaseClient<Database>; // anon key — NÃO usar em testes que validam RLS
```

Vitest config esperado:
```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react-swc';
import path from 'node:path';
export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    setupFiles: ['./vitest.setup.ts'],
    globals: true,
    coverage: { provider: 'v8', reporter: ['text', 'lcov'] },
  },
  resolve: { alias: { '@': path.resolve(__dirname, './src') } },
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar Vitest + helpers + criar config base</name>
  <files>package.json, vitest.config.ts, vitest.setup.ts, tsconfig.app.json</files>
  <action>
    1. Instalar dev deps:
       `npm install -D vitest @vitest/coverage-v8 @vitest/ui @testing-library/react @testing-library/jest-dom jsdom supertest @types/supertest`
    2. Criar `vitest.config.ts` exatamente como o snippet em <interfaces> (jsdom env, alias @ → src, setup file, coverage v8).
    3. Criar `vitest.setup.ts` com:
       ```typescript
       import '@testing-library/jest-dom/vitest';
       import { vi } from 'vitest';
       // Mock env vars usadas pelo cliente Supabase anon
       process.env.VITE_SUPABASE_URL ??= 'https://test.supabase.co';
       process.env.VITE_SUPABASE_PUBLISHABLE_KEY ??= 'test-anon-key';
       process.env.SUPABASE_SERVICE_ROLE_KEY ??= 'test-service-role';
       process.env.UPSTASH_REDIS_REST_URL ??= 'https://test-redis.upstash.io';
       process.env.UPSTASH_REDIS_REST_TOKEN ??= 'test-token';
       process.env.TURNSTILE_SECRET_KEY ??= 'test-turnstile-secret';
       ```
    4. Atualizar `package.json` adicionando scripts:
       ```json
       "test": "vitest run",
       "test:watch": "vitest",
       "test:coverage": "vitest run --coverage",
       "audit:no-supabase-from": "node scripts/audit-no-supabase-from.mjs"
       ```
    5. Atualizar `tsconfig.app.json` adicionando `"vitest/globals"` e `"@testing-library/jest-dom"` aos `types` (se ausente).

    Por que NÃO usar Jest: projeto é Vite 5; Vitest compartilha o transformer e dá feedback <1s. Jest exigiria Babel + ts-jest paralelo.
  </action>
  <verify>
    <automated>npm test -- --run --reporter=basic</automated>
  </verify>
  <done>`npm test -- --run` termina com exit 0 (mensagem "No test files found" ou equivalente) e `vitest.config.ts` + `vitest.setup.ts` existem com conteúdo correto.</done>
</task>

<task type="auto">
  <name>Task 2: Criar helpers de teste + audit script HARD-01</name>
  <files>tests/_helpers/handler.ts, tests/_helpers/supabase-mock.ts, tests/_helpers/db-staging.ts, scripts/audit-no-supabase-from.mjs</files>
  <action>
    1. Criar `tests/_helpers/handler.ts` — invoca Vercel Function handler sem subir `vercel dev`:
       ```typescript
       import type { VercelRequest, VercelResponse } from '@vercel/node';
       import { vi } from 'vitest';

       export type HandlerInput = {
         method?: string;
         query?: Record<string, string>;
         body?: unknown;
         headers?: Record<string, string>;
       };

       export async function invokeHandler(
         handler: (req: VercelRequest, res: VercelResponse) => unknown,
         input: HandlerInput = {}
       ) {
         const req = {
           method: input.method ?? 'GET',
           query: input.query ?? {},
           body: input.body,
           headers: input.headers ?? {},
         } as unknown as VercelRequest;

         let statusCode = 200;
         let jsonBody: unknown = null;
         const setHeader = vi.fn();
         const res = {
           status(code: number) { statusCode = code; return this; },
           json(b: unknown) { jsonBody = b; return this; },
           send(b: unknown) { jsonBody = b; return this; },
           setHeader,
           end() { return this; },
         } as unknown as VercelResponse;

         await handler(req, res);
         return { statusCode, body: jsonBody, setHeader };
       }
       ```
    2. Criar `tests/_helpers/supabase-mock.ts` — fluent mock chain compatível com `.from(...).select().eq().single()`:
       ```typescript
       import { vi } from 'vitest';

       export function makeSupabaseMock(overrides: Record<string, any> = {}) {
         const chain: any = {
           select: vi.fn(() => chain),
           insert: vi.fn(() => chain),
           upsert: vi.fn(() => chain),
           update: vi.fn(() => chain),
           delete: vi.fn(() => chain),
           eq: vi.fn(() => chain),
           in: vi.fn(() => chain),
           single: vi.fn(async () => ({ data: null, error: null })),
           maybeSingle: vi.fn(async () => ({ data: null, error: null })),
           then: undefined,
         };
         Object.assign(chain, overrides);
         return {
           from: vi.fn(() => chain),
           _chain: chain,
         };
       }
       ```
    3. Criar `tests/_helpers/db-staging.ts` (stub minimal — completar quando DB staging existir; por ora exporta connection string lida de env):
       ```typescript
       export const STAGING_DB_URL = process.env.SUPABASE_STAGING_DB_URL ?? '';
       export const STAGING_ANON_KEY = process.env.SUPABASE_STAGING_ANON_KEY ?? '';
       export function isStagingConfigured() {
         return STAGING_DB_URL.length > 0 && STAGING_ANON_KEY.length > 0;
       }
       ```
    4. Criar `scripts/audit-no-supabase-from.mjs`:
       ```javascript
       #!/usr/bin/env node
       import { execSync } from 'node:child_process';

       // Procura supabase.from('onboarding_sessions' | 'onboarding_respostas') em src/
       // EXCLUI src/integrations/ (cliente é declarado lá, mas não chamado)
       let out = '';
       try {
         out = execSync(
           'git grep -nE "supabase\\.from\\([\\\"\\x27]onboarding_(sessions|respostas)" -- src/ ":!src/integrations/"',
           { encoding: 'utf8' }
         );
       } catch (e) {
         // exit 1 = nenhum match (git grep retorna 1 quando não acha)
         if (e.status === 1) out = '';
         else throw e;
       }
       if (out.trim().length > 0) {
         console.error('FAIL: supabase.from(onboarding_*) detectado em src/ — viola HARD-01:');
         console.error(out);
         process.exit(1);
       }
       console.log('PASS: zero supabase.from(onboarding_*) em src/');
       process.exit(0);
       ```
    5. Tornar executável + verificar:
       ```bash
       chmod +x scripts/audit-no-supabase-from.mjs 2>/dev/null || true
       node scripts/audit-no-supabase-from.mjs
       ```

    NOTA: hoje (pre-Wave 1), `src/pages/Onboarding.tsx`, `src/pages/OnboardingSession.tsx`, `src/pages/NovoOnboarding.tsx` AINDA usam `supabase.from('onboarding_sessions')`. Audit script vai falhar nesta task — ESPERADO. Audit só fica green ao fim do Plan 03 (Wave 2). Para esta task, validar que script EXECUTA corretamente (acha as ocorrências), não que retorna green.
  </action>
  <verify>
    <automated>node scripts/audit-no-supabase-from.mjs; test $? -eq 1 && echo "EXPECTED_FAIL_OK" || echo "UNEXPECTED"</automated>
  </verify>
  <done>4 arquivos criados; `node scripts/audit-no-supabase-from.mjs` retorna exit 1 listando as ocorrências atuais em Onboarding.tsx/OnboardingSession.tsx/NovoOnboarding.tsx (comportamento esperado pré-migração).</done>
</task>

<task type="auto">
  <name>Task 3: Criar test stubs + CI workflow</name>
  <files>.github/workflows/ci.yml, api/sessions/__stubs__.test.ts, src/lib/__stubs__.test.ts, tests/rls/__stubs__.test.ts</files>
  <action>
    1. Criar stubs de teste vazios (failing OK) para garantir presença dos arquivos referenciados na VALIDATION.md. Usar pattern Vitest `it.todo`:

    `api/sessions/__stubs__.test.ts`:
    ```typescript
    import { describe, it } from 'vitest';
    describe('api/sessions Wave 1+ stubs', () => {
      it.todo('create.test.ts — POST /api/sessions/create cria session com slug+token');
      it.todo('get.test.ts — GET /api/sessions/get retorna estado por slug+token válidos');
      it.todo('save-answer.test.ts — PUT /api/sessions/save-resposta upsert idempotente');
      it.todo('save-answer.idempotency.test.ts — mesmo payload 2x = 1 row');
      it.todo('_lib/access.test.ts — assertSessionAccess valida TTL 30 dias');
      it.todo('advance-department.test.ts — gate 403 se identificação pendente');
      it.todo('create.ratelimit.test.ts — 6ª req/IP/min retorna 429');
    });
    ```

    `src/lib/__stubs__.test.ts`:
    ```typescript
    import { describe, it } from 'vitest';
    describe('src/lib + hooks Wave 2 stubs', () => {
      it.todo('sessionApi.test.ts — fetch wrappers tipados pra /api/sessions/*');
      it.todo('useAutosave.test.ts — debounce 500ms + cancela re-input');
      it.todo('TurnstileWidget.test.tsx — renderiza + callback onSuccess');
      it.todo('ProgressBar.test.tsx — denominador = 5');
      it.todo('idv-2026.test.tsx — paleta forest-floor + lime-accent presentes');
    });
    ```

    `tests/rls/__stubs__.test.ts`:
    ```typescript
    import { describe, it } from 'vitest';
    describe('tests/rls Wave 4 stubs', () => {
      it.todo('onboarding-sessions.test.ts — anon key SELECT/INSERT/UPDATE returns permission_denied pós-lock');
    });
    ```

    2. Criar `.github/workflows/ci.yml`:
    ```yaml
    name: CI
    on:
      pull_request:
      push: { branches: [main, migration/vercel] }
    jobs:
      test:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
            with: { fetch-depth: 0 } # git grep precisa de history
          - uses: actions/setup-node@v4
            with: { node-version: '20', cache: 'npm' }
          - run: npm ci
          - name: Audit HARD-01 (no supabase.from in src/)
            run: npm run audit:no-supabase-from
          - run: npm test -- --run --reporter=basic
    ```

    NOTA: workflow vai falhar no audit step até Wave 2 completar. Isso é esperado e DESEJADO — gate impede merge de progresso parcial. Felipe pode optar por `continue-on-error: true` no step de audit durante Wave 0/1; remover o flag quando Wave 2 fechar a migração.
  </action>
  <verify>
    <automated>npm test -- --run --reporter=basic 2>&1 | grep -E "todo|skipped|passed"</automated>
  </verify>
  <done>Stubs criados, CI workflow existe, `npm test -- --run` mostra `it.todo` skipped (suite green com 0 failures), audit script ainda detecta ocorrências antigas (gate aguardando Wave 2).</done>
</task>

</tasks>

<verification>
- `npm test -- --run` → exit 0 com tests skipped/todo
- `node scripts/audit-no-supabase-from.mjs` → exit 1 hoje (achou ocorrências), exit 0 após Plan 03
- `cat package.json | grep -E "vitest|audit:no-supabase"` → 4 scripts presentes
- `ls vitest.config.ts vitest.setup.ts tests/_helpers/handler.ts scripts/audit-no-supabase-from.mjs .github/workflows/ci.yml` → 5 arquivos existem
</verification>

<success_criteria>
Wave 0 complete quando:
- Vitest 2.x instalado e configurado (jsdom + alias @/)
- Helpers criados (handler, supabase-mock, db-staging)
- Audit script funcional (HARD-01 gate)
- CI workflow ativo
- Test stubs em place para Waves 1-4 (pattern `it.todo`)
- `nyquist_compliant: true` pode ser marcado em VALIDATION.md
</success_criteria>

<output>
Após completar, criar `.planning/phases/01-hardening-server-side-persistence/01-00-SUMMARY.md` documentando arquivos criados, scripts npm adicionados, e estado atual do audit (esperado falhar até Wave 2).
</output>
