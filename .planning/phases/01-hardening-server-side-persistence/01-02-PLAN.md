---
phase: 01-hardening-server-side-persistence
plan: 02
type: execute
wave: 1
depends_on: ["01-00"]
files_modified:
  - tailwind.config.ts
  - src/index.css
  - src/main.tsx
  - src/components/PipeeloLogo.tsx
  - src/styles/theme.ts
  - src/components/PipeeloLogo.test.tsx
  - src/styles/idv-2026.test.tsx
  - package.json
autonomous: true
requirements: [HARD-10]
must_haves:
  truths:
    - "Tailwind config tem tokens forest-floor (#000D0A) e lime-accent (#01d5ac) resolvíveis em classes utility"
    - "Inter font carregada via @fontsource/inter (sem Google Fonts)"
    - "PipeeloLogo renderiza SVG inline com fills IDV 2026 (sem dependência de PNG)"
    - "Snapshot test confirma paleta + fonte"
  artifacts:
    - path: "tailwind.config.ts"
      provides: "tokens forest-floor + lime-accent + fontFamily Inter"
      contains: "forest-floor"
    - path: "src/styles/theme.ts"
      provides: "Constantes COLORS exportadas para uso em tests + JS"
      exports: ["COLORS", "FOREST_FLOOR", "LIME_ACCENT"]
    - path: "src/components/PipeeloLogo.tsx"
      provides: "SVG inline IDV 2026 (substitui PNG legado)"
      exports: ["PipeeloLogo"]
    - path: "src/main.tsx"
      provides: "Imports @fontsource/inter weights 400/500/600/700"
  key_links:
    - from: "src/main.tsx"
      to: "@fontsource/inter"
      via: "side-effect import"
      pattern: "@fontsource/inter"
    - from: "tailwind.config.ts"
      to: "src/styles/theme.ts"
      via: "tokens compartilhados"
      pattern: "forest-floor|lime-accent"
---

<objective>
Aplicar IDV 2026 oficial: paleta Forest Floor `#000D0A` + accent `#01d5ac`, tipografia Inter (self-hosted), logo SVG inline. Independente do backend (HARD-10 paraleliza com HARD-01..09).

Purpose: Bug atual: tokens CSS aplicados em commit `ea79204` mas logo PNG (19/Apr) ainda é o legado. Inconsistência visual com brandbook.
Output: Tailwind tokens, font self-host, logo SVG, testes de snapshot.
</objective>

<execution_context>
@C:/Users/dopeb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dopeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-hardening-server-side-persistence/01-RESEARCH.md
@.planning/phases/01-hardening-server-side-persistence/01-VALIDATION.md
@tailwind.config.ts
@src/index.css
@src/main.tsx
@src/components/PipeeloLogo.tsx
@src/assets/pipeelo-logo.png

**Decisão locked (do RESEARCH):** Forest Floor `#000D0A` (background dark-first) + Lime Accent `#01d5ac`. Inter via `@fontsource/inter` (privacy + sem FOUT vs Google Fonts).

**Memory note (Felipe abr/2026):** Nova ID visual abr/2026 — Forest Floor escuro, Lime #7ACC42... CONFLITO: REQUIREMENTS HARD-10 diz `#01d5ac`. **Honrar HARD-10 (`#01d5ac`)** — está nos requirements travados; se Felipe quiser revisar pra `#7ACC42`, abrir como gap closure depois. Documentar essa decisão no SUMMARY.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar @fontsource/inter + adicionar tokens Tailwind + theme.ts</name>
  <files>package.json, tailwind.config.ts, src/styles/theme.ts, src/main.tsx</files>
  <action>
    1. Instalar:
    ```bash
    npm install @fontsource/inter
    ```

    2. Criar `src/styles/theme.ts` (fonte única de verdade para JS + tests):
    ```typescript
    export const FOREST_FLOOR = '#000D0A';
    export const LIME_ACCENT = '#01d5ac';
    export const LIME_ACCENT_HOVER = '#01b894';
    export const LIME_ACCENT_MUTED = '#3eedc5';

    export const COLORS = {
      'forest-floor': {
        DEFAULT: FOREST_FLOOR,
        50: '#0A1A16',
        100: '#0F2520',
        200: '#143028',
        300: '#1B3D32',
      },
      'lime-accent': {
        DEFAULT: LIME_ACCENT,
        hover: LIME_ACCENT_HOVER,
        muted: LIME_ACCENT_MUTED,
      },
    } as const;
    ```

    3. Atualizar `tailwind.config.ts` — adicionar (não substituir os tokens `pipeelo-*` existentes; coexistência durante transição):
    ```typescript
    import { COLORS } from './src/styles/theme';
    // dentro de theme.extend:
    extend: {
      colors: {
        ...existingColors,  // mantém pipeelo-purple etc
        ...COLORS,
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', '-apple-system', 'sans-serif'],
      },
    }
    ```

    4. `src/main.tsx` — adicionar imports de fonte (no topo, antes de App):
    ```typescript
    import '@fontsource/inter/400.css';
    import '@fontsource/inter/500.css';
    import '@fontsource/inter/600.css';
    import '@fontsource/inter/700.css';
    ```

    5. `src/index.css` — garantir body usa Inter + Forest Floor como background dark-first (verificar se commit ea79204 já fez; senão adicionar):
    ```css
    @layer base {
      html { font-family: 'Inter', system-ui, sans-serif; }
      body { @apply bg-forest-floor text-white; }
    }
    ```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tee /tmp/build.log; grep -E "error|Error" /tmp/build.log && exit 1 || exit 0</automated>
  </verify>
  <done>Build passa; @fontsource/inter em node_modules; tokens forest-floor e lime-accent em tailwind.config.ts; theme.ts exporta COLORS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Substituir PipeeloLogo por SVG inline + snapshot test</name>
  <files>src/components/PipeeloLogo.tsx, src/components/PipeeloLogo.test.tsx, src/styles/idv-2026.test.tsx</files>
  <behavior>
    - PipeeloLogo retorna `<svg>` inline (não `<img src=PNG>`)
    - Aceita prop `className` para sizing
    - Fill principal = LIME_ACCENT (#01d5ac)
    - Snapshot estável
    - idv-2026 test verifica COLORS.forest-floor.DEFAULT === '#000D0A' e COLORS.lime-accent.DEFAULT === '#01d5ac'
  </behavior>
  <action>
    1. Substituir `src/components/PipeeloLogo.tsx` (placeholder SVG; Felipe deve substituir pelo SVG oficial do brandbook se tiver):
    ```tsx
    import { LIME_ACCENT } from '@/styles/theme';

    type Props = { className?: string; fill?: string };

    export function PipeeloLogo({ className, fill = LIME_ACCENT }: Props) {
      return (
        <svg
          className={className}
          viewBox="0 0 200 60"
          xmlns="http://www.w3.org/2000/svg"
          aria-label="Pipeelo"
          role="img"
        >
          <text
            x="0" y="42"
            fontFamily="Inter, system-ui, sans-serif"
            fontWeight="700"
            fontSize="40"
            fill={fill}
          >
            pipeelo
          </text>
        </svg>
      );
    }

    export default PipeeloLogo;
    ```

    NOTA: este é placeholder. Se Felipe tem SVG oficial em `~/Desktop/agentes-ia-prompts/...` ou Figma, substituir paths/transforms. Manter API (props className, fill) idêntica para não quebrar consumers.

    2. `src/components/PipeeloLogo.test.tsx`:
    ```tsx
    import { describe, it, expect } from 'vitest';
    import { render } from '@testing-library/react';
    import { PipeeloLogo } from './PipeeloLogo';
    import { LIME_ACCENT } from '@/styles/theme';

    describe('<PipeeloLogo />', () => {
      it('renderiza SVG inline (não img)', () => {
        const { container } = render(<PipeeloLogo />);
        expect(container.querySelector('svg')).toBeInTheDocument();
        expect(container.querySelector('img')).toBeNull();
      });
      it('usa fill LIME_ACCENT por default', () => {
        const { container } = render(<PipeeloLogo />);
        const text = container.querySelector('text');
        expect(text?.getAttribute('fill')).toBe(LIME_ACCENT);
      });
      it('aceita className prop', () => {
        const { container } = render(<PipeeloLogo className="h-8 w-auto" />);
        expect(container.querySelector('svg')?.getAttribute('class')).toContain('h-8');
      });
      it('snapshot estável', () => {
        const { container } = render(<PipeeloLogo />);
        expect(container.innerHTML).toMatchSnapshot();
      });
    });
    ```

    3. `src/styles/idv-2026.test.tsx`:
    ```tsx
    import { describe, it, expect } from 'vitest';
    import { COLORS, FOREST_FLOOR, LIME_ACCENT } from './theme';

    describe('IDV 2026 tokens', () => {
      it('Forest Floor é #000D0A', () => {
        expect(FOREST_FLOOR).toBe('#000D0A');
        expect(COLORS['forest-floor'].DEFAULT).toBe('#000D0A');
      });
      it('Lime Accent é #01d5ac', () => {
        expect(LIME_ACCENT).toBe('#01d5ac');
        expect(COLORS['lime-accent'].DEFAULT).toBe('#01d5ac');
      });
      it('Lime tem variantes hover + muted', () => {
        expect(COLORS['lime-accent'].hover).toBeDefined();
        expect(COLORS['lime-accent'].muted).toBeDefined();
      });
    });
    ```

    4. Remover ou manter PNG legado? **Manter `src/assets/pipeelo-logo.png` por ora** (rollback safety durante deploy preview). Apagar em Phase 5 quando IDV 2026 estiver validada visualmente.
  </action>
  <verify>
    <automated>npm test -- --run src/components/PipeeloLogo.test.tsx src/styles/idv-2026.test.tsx</automated>
  </verify>
  <done>4 testes PipeeloLogo passam; 3 testes idv-2026 passam; snapshot file gerado em `__snapshots__/`.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 3: Visual verification IDV 2026 em deploy preview</name>
  <what-built>
    Tokens Forest Floor (#000D0A) + Lime Accent (#01d5ac) aplicados via Tailwind, Inter self-hosted via @fontsource, PipeeloLogo SVG inline.
  </what-built>
  <how-to-verify>
    1. Rodar `npm run dev` localmente
    2. Abrir http://localhost:5173/
    3. Inspector DevTools → selecionar `body` → confirmar:
       - background-color: rgb(0, 13, 10) (= #000D0A)
       - font-family: Inter, system-ui...
    4. Confirmar logo Pipeelo no header é o SVG novo (verde lime), não o PNG legado roxo
    5. Navegar para `/novo` (NovoOnboarding) → confirmar Forest Floor + Lime + Inter consistentes
    6. (OPCIONAL) Comparar com brandbook 2026 que Felipe tem em `~/Desktop/agentes-ia-prompts/`
  </how-to-verify>
  <resume-signal>Type "approved" se IDV correta, ou descrever ajustes (paths SVG, weight Inter, hex shift, etc)</resume-signal>
</task>

</tasks>

<verification>
- `npm test -- --run src/components/PipeeloLogo src/styles/idv-2026` → 7 testes passam
- `npm run build` → build green com @fontsource/inter chunks
- Visual check em dev (checkpoint humano)
</verification>

<success_criteria>
HARD-10 complete:
- Forest Floor + Lime Accent disponíveis como classes Tailwind (`bg-forest-floor`, `text-lime-accent`)
- Inter self-hosted (sem Google Fonts CDN)
- Logo PipeeloLogo é SVG inline com fill IDV 2026
- Testes garantem regressão protegida
</success_criteria>

<output>
Após completar, criar `.planning/phases/01-hardening-server-side-persistence/01-02-SUMMARY.md` documentando tokens exatos, decisão `#01d5ac` (vs Felipe memory `#7ACC42`), e SVG paths para Wave 2 reutilizar.
</output>
