---
phase: 01-hardening-server-side-persistence
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - supabase/migrations/20260508120000_lock_rls_phase1.sql
  - scripts/rollback-rls.sql
  - tests/rls/onboarding-sessions.test.ts
  - tests/_helpers/db-staging.ts
  - .planning/phases/01-hardening-server-side-persistence/01-05-RUNBOOK.md
autonomous: false
requirements: [HARD-08, HARD-09]
user_setup:
  - service: supabase
    why: "Aplicar migration de lock + smoke test em staging antes de prod"
    env_vars:
      - name: SUPABASE_STAGING_DB_URL
        source: "Supabase Dashboard → Project Settings → Database → Connection String"
      - name: SUPABASE_STAGING_ANON_KEY
        source: "Supabase Dashboard → Project Settings → API → anon public"
    dashboard_config:
      - task: "Confirmar que branch DB de staging está sincronizada com schema de prod (mesmas migrations)"
        location: "Supabase Dashboard → Database → Migrations"
must_haves:
  truths:
    - "Migration `20260508120000_lock_rls_phase1.sql` aplica policy `service_role only AS RESTRICTIVE` em onboarding_sessions e onboarding_respostas"
    - "Cliente anon SELECT em onboarding_sessions retorna 0 rows (ou permission denied) pós-lock"
    - "Cliente anon INSERT/UPDATE em onboarding_sessions retorna PostgrestError code 42501"
    - "scripts/rollback-rls.sql reverte lock em <5 minutos (recreate public policies)"
    - "Smoke staging end-to-end pós-lock confirma fluxo completo funciona via service-role"
    - "RUNBOOK documenta sequência migrate-then-lock + rollback procedure"
  artifacts:
    - path: "supabase/migrations/20260508120000_lock_rls_phase1.sql"
      provides: "Lock RLS migration — drop public policies + recreate service_role only restritiva"
      contains: "AS RESTRICTIVE"
    - path: "scripts/rollback-rls.sql"
      provides: "SQL reverso aplicável manualmente em <5min"
      contains: "CREATE POLICY"
    - path: "tests/rls/onboarding-sessions.test.ts"
      provides: "Integração: anon key denied (HARD-09)"
    - path: ".planning/phases/01-hardening-server-side-persistence/01-05-RUNBOOK.md"
      provides: "Runbook de aplicação + rollback drill"
  key_links:
    - from: "supabase/migrations/20260508120000_lock_rls_phase1.sql"
      to: "Tabelas onboarding_sessions + onboarding_respostas"
      via: "DROP POLICY public + CREATE POLICY service_role only AS RESTRICTIVE"
      pattern: "AS RESTRICTIVE"
    - from: "tests/rls/onboarding-sessions.test.ts"
      to: "Supabase staging anon client"
      via: "createClient(STAGING_DB_URL, STAGING_ANON_KEY) → expectations: 0 rows / permission_denied"
      pattern: "createClient.*ANON"
---

<objective>
Aplicar migration de aperto de RLS, validar com teste de integração via anon key, e executar rollback drill em staging. Esta é a wave de cutover crítica — TODOS os planos anteriores precisam estar verdes antes.

Purpose: Plan 03 fechou audit HARD-01. Plan 04 hardenou endpoints. Sem este plan, RLS continua afrouxado em prod (leak ATIVO). Sequência migrate-then-lock (Pitfall 4) é INEGOCIÁVEL.
Output: Migration SQL + rollback SQL + integration test + RUNBOOK + checkpoint humano de aplicação.
</objective>

<execution_context>
@C:/Users/dopeb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dopeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-hardening-server-side-persistence/01-RESEARCH.md
@.planning/phases/01-hardening-server-side-persistence/01-VALIDATION.md
@.planning/phases/01-hardening-server-side-persistence/01-03-SUMMARY.md
@.planning/phases/01-hardening-server-side-persistence/01-04-SUMMARY.md
@supabase/migrations/20260419000000_identificacao_and_tenant_link.sql
@supabase/migrations/20260419120000_relax_rls_for_testing.sql

<interfaces>
Migration original (a recriar AS RESTRICTIVE):
```sql
-- supabase/migrations/20260419000000_identificacao_and_tenant_link.sql (linhas 39-47)
CREATE POLICY "service_role only" ON public.onboarding_sessions
  AS RESTRICTIVE FOR ALL TO anon, authenticated
  USING (false) WITH CHECK (false);
```

Migration que afrouxou (a reverter):
```sql
-- supabase/migrations/20260419120000_relax_rls_for_testing.sql
-- Criou policies "public read sessions", "public insert sessions", "public update sessions"
-- + mesmas em onboarding_respostas
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration de lock + rollback SQL + RUNBOOK</name>
  <files>supabase/migrations/20260508120000_lock_rls_phase1.sql, scripts/rollback-rls.sql, .planning/phases/01-hardening-server-side-persistence/01-05-RUNBOOK.md</files>
  <action>
    1. `supabase/migrations/20260508120000_lock_rls_phase1.sql`:
    ```sql
    -- Phase 1 HARD-08/HARD-09: revert relax_rls_for_testing.sql
    -- Lock RLS to service_role only on onboarding_sessions + onboarding_respostas
    -- Pré-condições (validar antes de aplicar):
    --   1. Audit script HARD-01 verde: zero supabase.from(onboarding_*) em src/
    --   2. Smoke staging completou sessão end-to-end via /api/sessions/*
    --   3. Equipe sabe do scripts/rollback-rls.sql

    BEGIN;

    -- Drop public policies criadas em 20260419120000_relax_rls_for_testing.sql
    DROP POLICY IF EXISTS "public read sessions"   ON public.onboarding_sessions;
    DROP POLICY IF EXISTS "public insert sessions" ON public.onboarding_sessions;
    DROP POLICY IF EXISTS "public update sessions" ON public.onboarding_sessions;

    DROP POLICY IF EXISTS "public read respostas"   ON public.onboarding_respostas;
    DROP POLICY IF EXISTS "public insert respostas" ON public.onboarding_respostas;
    DROP POLICY IF EXISTS "public update respostas" ON public.onboarding_respostas;

    -- Drop service_role policies anteriores se existirem (idempotente)
    DROP POLICY IF EXISTS "service_role only" ON public.onboarding_sessions;
    DROP POLICY IF EXISTS "service_role only" ON public.onboarding_respostas;

    -- Recreate service_role only AS RESTRICTIVE (igual 20260419000000)
    CREATE POLICY "service_role only" ON public.onboarding_sessions
      AS RESTRICTIVE FOR ALL
      TO anon, authenticated
      USING (false)
      WITH CHECK (false);

    CREATE POLICY "service_role only" ON public.onboarding_respostas
      AS RESTRICTIVE FOR ALL
      TO anon, authenticated
      USING (false)
      WITH CHECK (false);

    -- Garante RLS habilitado (não-op se já estava)
    ALTER TABLE public.onboarding_sessions ENABLE ROW LEVEL SECURITY;
    ALTER TABLE public.onboarding_respostas ENABLE ROW LEVEL SECURITY;

    COMMIT;

    -- Verificação manual (rodar separado após apply):
    --   SELECT tablename, policyname, permissive, roles, qual, with_check
    --   FROM pg_policies WHERE tablename IN ('onboarding_sessions','onboarding_respostas');
    -- Esperado: única policy 'service_role only', permissive=RESTRICTIVE, qual='false'
    ```

    2. `scripts/rollback-rls.sql`:
    ```sql
    -- ROLLBACK Phase 1 lock: recreate public policies (rede de segurança)
    -- USO: aplicar manualmente em incidente onde lock quebrou prod
    --   psql $SUPABASE_DB_URL -f scripts/rollback-rls.sql
    -- IMPORTANTE: aplicar SOMENTE se monitoramento mostrar 401/403 em massa pós-lock

    BEGIN;

    DROP POLICY IF EXISTS "service_role only" ON public.onboarding_sessions;
    DROP POLICY IF EXISTS "service_role only" ON public.onboarding_respostas;

    -- Recreate as policies de relax (mesma forma de 20260419120000_relax_rls_for_testing.sql)
    CREATE POLICY "public read sessions" ON public.onboarding_sessions
      FOR SELECT TO anon, authenticated USING (true);
    CREATE POLICY "public insert sessions" ON public.onboarding_sessions
      FOR INSERT TO anon, authenticated WITH CHECK (true);
    CREATE POLICY "public update sessions" ON public.onboarding_sessions
      FOR UPDATE TO anon, authenticated USING (true) WITH CHECK (true);

    CREATE POLICY "public read respostas" ON public.onboarding_respostas
      FOR SELECT TO anon, authenticated USING (true);
    CREATE POLICY "public insert respostas" ON public.onboarding_respostas
      FOR INSERT TO anon, authenticated WITH CHECK (true);
    CREATE POLICY "public update respostas" ON public.onboarding_respostas
      FOR UPDATE TO anon, authenticated USING (true) WITH CHECK (true);

    COMMIT;

    -- Após rollback: investigar causa raiz, NÃO redeployar lock até descobrir o miss.
    ```

    3. `.planning/phases/01-hardening-server-side-persistence/01-05-RUNBOOK.md`:
    ```markdown
    # Runbook — Phase 1 Lock RLS Migration

    **Risco:** ALTO — RLS aperta em produção, pode quebrar fluxo onboarding se houver miss em qualquer página/endpoint.
    **Tempo total estimado:** 30 minutos (incluindo smoke + rollback drill em staging).

    ## Pré-condições obrigatórias (TODAS green)

    - [ ] `npm test -- --run` → todos os testes verdes
    - [ ] `npm run audit:no-supabase-from` → exit 0
    - [ ] Plan 01-03 SUMMARY confirma: 3 pages refatoradas
    - [ ] Plan 01-04 SUMMARY confirma: rate-limit + Turnstile + BrasilAPI funcionais
    - [ ] Vercel preview deploy em branch atual está verde
    - [ ] `SUPABASE_STAGING_DB_URL` + `SUPABASE_STAGING_ANON_KEY` configurados em `.env.local`

    ## Sequência de aplicação

    ### Etapa 1 — Smoke staging (PRE-LOCK)
    Confirmar que sessão funciona ANTES de apertar. Se falhar aqui, problema não é RLS.

    1. Deploy preview da branch atual no Vercel apontando para Supabase staging
    2. Criar sessão em `https://<preview>.vercel.app/novo` com CNPJ teste
    3. Preencher 5 perguntas de Identificação
    4. Concluir Identificação → confirmar `status_identificacao=concluido` no Supabase
    5. Avançar para Financeiro → preencher 3 perguntas → fechar aba
    6. Reabrir magic link recebido por email (ou copiar URL com `?token=`)
    7. Confirmar respostas presentes
    8. **GATE:** se passar → seguir. Se falhar → corrigir bug, NÃO continuar.

    ### Etapa 2 — Aplicar lock em STAGING

    ```bash
    # No diretório do projeto:
    psql "$SUPABASE_STAGING_DB_URL" -f supabase/migrations/20260508120000_lock_rls_phase1.sql

    # Verificar policies pós-lock:
    psql "$SUPABASE_STAGING_DB_URL" -c "SELECT tablename, policyname, permissive, roles FROM pg_policies WHERE tablename LIKE 'onboarding_%';"
    # Esperado: 1 policy 'service_role only' por tabela, permissive=RESTRICTIVE
    ```

    ### Etapa 3 — Validar lock em staging
    1. Rodar `npm test -- --run tests/rls/onboarding-sessions.test.ts` (SUPABASE_STAGING_* env)
    2. Esperado: anon SELECT retorna 0 rows OU permission denied; INSERT/UPDATE retornam erro 42501
    3. Refazer smoke (Etapa 1, passos 2-7) com staging pós-lock
    4. **GATE:** se passar → testar rollback drill (Etapa 4). Se falhar → rollback imediato (Etapa 5).

    ### Etapa 4 — Rollback drill (em staging)
    Confirma que rollback funciona ANTES de aplicar em prod.

    ```bash
    psql "$SUPABASE_STAGING_DB_URL" -f scripts/rollback-rls.sql
    # Re-aplicar lock:
    psql "$SUPABASE_STAGING_DB_URL" -f supabase/migrations/20260508120000_lock_rls_phase1.sql
    ```

    Tempo medido: ____ segundos. Se >5min → investigar.

    ### Etapa 5 — Aplicar em PROD

    **CHECKPOINT HUMANO** (Felipe confirma):

    ```bash
    psql "$SUPABASE_PROD_DB_URL" -f supabase/migrations/20260508120000_lock_rls_phase1.sql
    ```

    Janela de monitoramento: 30 minutos pós-deploy.
    - Vercel logs: filtrar 401/403/permission_denied
    - Supabase logs: filtrar policy violations
    - Sentry/console: spike de erros

    ### Etapa 6 — Rollback prod (somente se incidente)

    ```bash
    psql "$SUPABASE_PROD_DB_URL" -f scripts/rollback-rls.sql
    ```

    Investigar causa raiz. Não re-aplicar lock até identificar o miss.

    ## Smoke checklist pós-lock prod

    - [ ] Criar sessão de teste em `/novo` (Felipe via celular ou colaborador)
    - [ ] Preencher Identificação → status_identificacao = concluido
    - [ ] Logar magic link em janela anônima → respostas carregam
    - [ ] Tentar avançar para Financeiro → cards renderizam (gate UI funciona)
    - [ ] Concluir Financeiro → status_financeiro = concluido
    - [ ] Verificar `pg_policies` em prod confirma 'service_role only' RESTRICTIVE
    ```
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260508120000_lock_rls_phase1.sql && test -f scripts/rollback-rls.sql && grep -q "AS RESTRICTIVE" supabase/migrations/20260508120000_lock_rls_phase1.sql</automated>
  </verify>
  <done>3 arquivos criados; migration tem `AS RESTRICTIVE`; rollback é idempotente; RUNBOOK detalha sequência completa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test — anon key denied pós-lock (HARD-09)</name>
  <files>tests/rls/onboarding-sessions.test.ts, tests/_helpers/db-staging.ts</files>
  <behavior>
    - createClient com anon key staging
    - SELECT * FROM onboarding_sessions → 0 rows (RESTRICTIVE policy with USING false)
    - INSERT → error code 42501 / permission denied
    - UPDATE → error code 42501
    - Mesmo para onboarding_respostas
    - Skip se isStagingConfigured() === false (CI ainda passa em PR sem env)
  </behavior>
  <action>
    1. Atualizar `tests/_helpers/db-staging.ts` (já existe stub do Plan 00):
    ```typescript
    import { createClient, type SupabaseClient } from '@supabase/supabase-js';

    export const STAGING_DB_URL = process.env.SUPABASE_STAGING_DB_URL ?? '';
    export const STAGING_URL = process.env.SUPABASE_STAGING_URL ?? '';
    export const STAGING_ANON_KEY = process.env.SUPABASE_STAGING_ANON_KEY ?? '';

    export function isStagingConfigured(): boolean {
      return STAGING_URL.length > 0 && STAGING_ANON_KEY.length > 0;
    }

    export function makeStagingAnonClient(): SupabaseClient {
      if (!isStagingConfigured()) throw new Error('staging_not_configured');
      return createClient(STAGING_URL, STAGING_ANON_KEY);
    }
    ```

    2. `tests/rls/onboarding-sessions.test.ts`:
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { isStagingConfigured, makeStagingAnonClient } from '../_helpers/db-staging';

    const skip = !isStagingConfigured();
    const d = skip ? describe.skip : describe;

    d('RLS lock — anon key denied em onboarding_sessions/respostas', () => {
      const anon = isStagingConfigured() ? makeStagingAnonClient() : null;

      it('SELECT * onboarding_sessions retorna 0 rows OU permission_denied', async () => {
        const { data, error } = await anon!.from('onboarding_sessions').select('*').limit(1);
        // Aceitamos: data=[] (RESTRICTIVE com USING false retorna 0 rows) OU error PostgrestError 42501
        if (error) {
          expect(error.code).toBe('42501');
        } else {
          expect(data).toEqual([]);
        }
      });

      it('INSERT onboarding_sessions retorna 42501 permission_denied', async () => {
        const { error } = await anon!.from('onboarding_sessions').insert({
          slug: 'test-' + Date.now(),
          access_token: 'x'.repeat(32),
          empresa_nome: 'Test',
          cnpj: '11222333000181',
        });
        expect(error).not.toBeNull();
        expect(error!.code).toBe('42501');
      });

      it('UPDATE onboarding_sessions retorna 42501 ou affecta 0 rows', async () => {
        const { error, count } = await anon!
          .from('onboarding_sessions')
          .update({ empresa_nome: 'hacked' })
          .eq('slug', 'any')
          .select('*', { count: 'exact', head: true });
        if (error) expect(error.code).toBe('42501');
        else expect(count ?? 0).toBe(0);
      });

      it('SELECT * onboarding_respostas retorna 0 rows OU permission_denied', async () => {
        const { data, error } = await anon!.from('onboarding_respostas').select('*').limit(1);
        if (error) expect(error.code).toBe('42501');
        else expect(data).toEqual([]);
      });

      it('INSERT onboarding_respostas retorna 42501', async () => {
        const { error } = await anon!.from('onboarding_respostas').insert({
          session_id: '00000000-0000-0000-0000-000000000000',
          departamento: 'identificacao',
          pergunta_id: 'q1',
          valor: 'v',
        });
        expect(error).not.toBeNull();
        expect(error!.code).toBe('42501');
      });
    });
    ```

    NOTA: este teste só roda se `SUPABASE_STAGING_URL` + `SUPABASE_STAGING_ANON_KEY` estiverem em env. Em CI sem esses secrets, suite skipa (não falha). O teste rodará pós-aplicação da migration em staging (Etapa 3 do RUNBOOK).
  </action>
  <verify>
    <automated>npm test -- --run tests/rls/onboarding-sessions.test.ts</automated>
  </verify>
  <done>Test file existe; sem env staging skipa graciosamente; com env, valida 5 cenários (3 sessions + 2 respostas).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHECKPOINT — Aplicar migration de lock + smoke staging + rollback drill + cutover prod</name>
  <what-built>
    Migration `20260508120000_lock_rls_phase1.sql` + `scripts/rollback-rls.sql` + integration test `tests/rls/onboarding-sessions.test.ts` + RUNBOOK detalhado.
    Audit HARD-01 verde, todos endpoints `/api/sessions/*` funcionais, rate-limit + Turnstile + BrasilAPI ativos.
  </what-built>
  <how-to-verify>
    Seguir `01-05-RUNBOOK.md` na íntegra. Resumo:

    **PRÉ-CONDIÇÕES (rodar antes de chamar Felipe):**
    1. `npm test -- --run` → exit 0
    2. `npm run audit:no-supabase-from` → exit 0
    3. `cat .planning/phases/01-hardening-server-side-persistence/01-03-SUMMARY.md` → confirmar pages migradas
    4. `cat .planning/phases/01-hardening-server-side-persistence/01-04-SUMMARY.md` → confirmar Wave 3 complete

    **EXECUÇÃO COM FELIPE (presente):**
    1. **Smoke staging PRE-LOCK** (RUNBOOK Etapa 1) — completar sessão end-to-end com magic link
    2. **Aplicar lock em staging** (Etapa 2) — `psql $SUPABASE_STAGING_DB_URL -f supabase/migrations/20260508120000_lock_rls_phase1.sql`
    3. **Rodar test integração** (Etapa 3) — `npm test -- --run tests/rls/onboarding-sessions.test.ts` com env staging → green
    4. **Smoke staging POST-LOCK** (Etapa 3) — refazer sessão completa funcionando
    5. **Rollback drill** (Etapa 4) — aplicar `scripts/rollback-rls.sql` + re-apply migration → tempo medido <5min
    6. **APLICAR EM PROD** (Etapa 5) — Felipe confirma window de baixo tráfego (manhã BRT)
       ```bash
       psql "$SUPABASE_PROD_DB_URL" -f supabase/migrations/20260508120000_lock_rls_phase1.sql
       ```
    7. **Smoke prod** (Etapa 6 checklist) — sessão real de teste pós-deploy
    8. **Monitorar 30min** — Vercel logs + Supabase logs filtrando 401/403/42501

    **Critérios para "approved":**
    - [ ] Smoke staging PRE-LOCK passou
    - [ ] Migration aplicou sem erro em staging
    - [ ] tests/rls/onboarding-sessions.test.ts verde
    - [ ] Smoke staging POST-LOCK passou
    - [ ] Rollback drill <5min
    - [ ] Migration aplicou sem erro em PROD
    - [ ] Smoke prod passou
    - [ ] 30min monitoramento sem spike anormal de erros

    **Se qualquer falhar:** rollback IMEDIATO (Etapa 5/6 RUNBOOK), investigar antes de re-aplicar.
  </how-to-verify>
  <resume-signal>Type "approved" se todos critérios atendidos em prod, ou "rolled-back: <razão>" se precisou reverter (Phase 1 fica em estado parcial até Plan 06 de gap closure).</resume-signal>
</task>

</tasks>

<verification>
- Migration aplicada em prod sem incidente
- `pg_policies` em prod mostra apenas `service_role only` RESTRICTIVE
- tests/rls suite verde com env staging
- Smoke prod passou
- Audit HARD-01 verde mantido
</verification>

<success_criteria>
HARD-08 + HARD-09 complete:
- Lock migration aplicada em prod
- Anon key bloqueada para onboarding_sessions + onboarding_respostas (validado por integration test + smoke)
- Rollback procedure testada e documentada
- Phase 1 inteira: 10/10 requirements green
</success_criteria>

<output>
Após approval Felipe, criar `.planning/phases/01-hardening-server-side-persistence/01-05-SUMMARY.md` documentando:
- Timestamp da migration em prod
- Output de `pg_policies` confirmando lock
- Resultados do smoke staging + prod
- Tempo do rollback drill
- Window de monitoramento sem incidentes
- Tags ROADMAP atualizadas: HARD-01..10 todos marcados Pending → Done
- STATE.md atualizado: Phase 1 → Done, próximo Phase 2
</output>
