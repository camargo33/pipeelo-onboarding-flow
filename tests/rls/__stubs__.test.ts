import { describe, it } from 'vitest';

describe('tests/rls Wave 4 stubs', () => {
  it.todo(
    'onboarding-sessions.test.ts — anon key SELECT/INSERT/UPDATE returns permission_denied pós-lock'
  );
});
